#include <iostream>
{% for field in project.fields %}
#include "{{field.fieldDataClassName}}.h"
{% endfor %}
#include "{{project.name}}Data.h"
int makeData({{femDataClassName}}& data) {
    // TODO: 添加测试数据
{% for mt in project.mateTypes %}
    // 本构模型 {{ mt.name }}: 参数 {{ mt.params | join(", ") }}
{% endfor %}

    return 0;
}

int main() {
    {{femDataClassName}} data;
    makeData(data);
    data.caculate();
    return 0;
}
