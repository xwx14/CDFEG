#include <iostream>
#include "Elastic2DDispFieldData.h"
#include "Elastic2DData.h"
int makeData(Elastic2DData& data) {
    // TODO: 添加测试数据


    return 0;
}

int main() {
    Elastic2DData data;
    makeData(data);
    data.caculate();
    return 0;
}