# CDFEG 测试系统 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 CDFEG 构建一套以回归测试为核心的本地测试系统：改基础库后一条命令量化全示例输出漂移，守住计算结果稳定性。

**Architecture:** 纯 Python 编排器（`test/`），通过 subprocess 编排"构建→运行→对比→报告"。框架依赖统一 `Case` 抽象，四层套件（e2e/unit/generator/analytical）是其实现。e2e 用 GiD `.post.res` 数值解析对比（结构一致性优先 + 逐点容差），双层基准锚定（regression 1e-12 / accuracy 1e-10）。Tracer-bullet：先打通 `del2d1` 单用例全链路，再横向扩展。

**Tech Stack:** Python ≥3.11（`tomllib`）、pytest（框架自测）、subprocess；C++14 + Catch2 v2 单头（unit 套件）；CMake + cygwin Unix Makefiles（构建）。

**关联设计：** [`docs/superpowers/specs/2026-07-17-测试系统-design.md`](../specs/2026-07-17-测试系统-design.md)

## Global Constraints

- **构建工具链**：cygwin + `Unix Makefiles`，构建根 = `FEMproject/`，可执行落 `test/build/output/`。
- **C++ 标准**：C++14；MSVC 需 `/utf-8`（根 CMake 已配）；cygwin 无需。
- **命名**：变量函数驼峰；C++ 成员 `_` 开头；文档中文命名；git 提交注释中文。
- **编码**：源文件 UTF-8（含中文注释）。
- **构建输出统一**：可执行与 DLL 统一到 `build/out`（测试构建即 `test/build/output/`）。
- **C++ 头文件防御**：`#ifndef/#define/#endif`（不用 `pragma once`）。
- **测试系统不侵入核心库源码**：唯一改动是 `FEMproject/CMakeLists.txt` 加默认 OFF 的 `CDFEG_BUILD_TESTS` option。
- **基准更新安全**：禁用 `--update all`，必须逐用例 + 强制 diff 确认。

---

## File Structure

**新建文件：**

| 文件 | 职责 |
|---|---|
| `test/run_tests.py` | 唯一 CLI 入口：解析参数、读 config、装配 Case、聚合报告、返回退出码 |
| `test/config.toml` | 工具链/路径/容差/各套件用例注册 |
| `test/framework/__init__.py` | 包标识 |
| `test/framework/tolerance.py` | `Tolerance` 数据类（atol/rtol） |
| `test/framework/config.py` | 读 `config.toml` → 结构化配置对象 |
| `test/framework/parser.py` | GiD `.post.res` 解析 → `dict[(name,step), ResBlock]` |
| `test/framework/comparator.py` | 两段判定：结构一致性 + 数值逐点对比 → `CompareResult` |
| `test/framework/builder.py` | cmake configure + build，target 缓存，返回 exe 路径 |
| `test/framework/runner.py` | subprocess 跑 exe，收集产出，超时控制 |
| `test/framework/report.py` | 聚合 `CaseResult` 列表，终端报告，退出码 |
| `test/framework/case.py` | `Case` 协议、`CaseResult`、`E2ECase`/`UnitCase`/`GeneratorCase`/`AnalyticalCase` |
| `test/framework/tests/test_*.py` | 框架自身 pytest 单测（parser/comparator/config/builder/runner/report） |
| `test/suites/unit/CMakeLists.txt` | 编译 Catch2 单测，链接 CDFEG 库 |
| `test/suites/unit/catch.hpp` | Catch2 v2 单头（下载） |
| `test/suites/unit/catch_main.cpp` | Catch2 main |
| `test/suites/unit/test_*.cpp` | 核心库机制断言 |
| `test/suites/generator/test_*.py` | pyTool 解析正确性 pytest |
| `test/models/` | 黄金基准（`del2d1`/`hel2d1` 已存在） |

**修改文件：**

| 文件 | 改动 |
|---|---|
| `FEMproject/CMakeLists.txt` | 末尾加 `CDFEG_BUILD_TESTS` option + `add_subdirectory(test/suites/unit)` |
| `.gitignore` | 追加 `test/build/`、`test/reports/` |

---

## 阶段 A — Python 框架（纯逻辑，TDD，无构建依赖）

### Task 1: Python 包骨架 + .gitignore + 依赖声明

**Files:**
- Create: `test/framework/__init__.py`
- Create: `test/requirements.txt`
- Modify: `.gitignore`
- Test: 无（骨架）

**Interfaces:**
- Produces: `test/framework/` 为可导入 Python 包；`.gitignore` 忽略 `test/build/`、`test/reports/`。

- [ ] **Step 1: 创建包标识**

创建 `test/framework/__init__.py`（空文件）：
```python
# CDFEG 测试系统编排框架
```

- [ ] **Step 2: 声明依赖**

创建 `test/requirements.txt`：
```
# 框架自测用 pytest；tomllib 为 Python 3.11+ 标准库，无需列出
pytest>=7.0
```

- [ ] **Step 3: 更新 .gitignore**

在 `.gitignore` 末尾追加（若已存在则跳过）：
```
# 测试系统构建与报告产物
test/build/
test/reports/
```

- [ ] **Step 4: 验证包可导入**

Run: `cd E:/myProject/CDFEG && python -c "import test.framework; print('ok')"`
Expected: 输出 `ok`（注：需 `test/__init__.py`，下一步处理；若报错先建空 `test/__init__.py`）

- [ ] **Step 5: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/__init__.py test/__init__.py test/requirements.txt .gitignore
git commit -m "test: 初始化测试系统 Python 包骨架"
```

---

### Task 2: tolerance.py + config.py（配置读取）

**Files:**
- Create: `test/framework/tolerance.py`
- Create: `test/framework/config.py`
- Create: `test/config.toml`
- Test: `test/framework/tests/test_config.py`

**Interfaces:**
- Produces: `Tolerance(atol: float, rtol: float)`；`load_config(path) -> Config`（含 `toolchain`、`suite` 字典）；config.toml schema。

- [ ] **Step 1: 写失败测试**

创建 `test/framework/tests/test_config.py`：
```python
import pytest
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))  # test/ 入 path
from framework.tolerance import Tolerance
from framework.config import load_config


def test_tolerance_defaults():
    t = Tolerance(atol=1e-12)
    assert t.atol == 1e-12
    assert t.rtol == 0.0


def test_load_config_reads_toolchain(tmp_path):
    cfg_file = tmp_path / "config.toml"
    cfg_file.write_text(
        '[toolchain]\n'
        'cmake_generator = "Unix Makefiles"\n'
        'source_dir = "FEMproject"\n'
        'build_dir = "test/build"\n'
        'output_subdir = "output"\n',
        encoding="utf-8",
    )
    cfg = load_config(cfg_file)
    assert cfg.toolchain.cmake_generator == "Unix Makefiles"
    assert cfg.toolchain.source_dir == "FEMproject"


def test_load_config_reads_e2e_cases(tmp_path):
    cfg_file = tmp_path / "config.toml"
    cfg_file.write_text(
        '[[suite.e2e.cases]]\n'
        'name = "del2d1"\n'
        'target = "del2d"\n'
        'project = "del2d"\n'
        'case_dir = "models/del2d1.gid"\n'
        'baseline = "del2d.post.res"\n'
        'output = "del2d.post.res"\n'
        'tol_atol = 1e-12\n',
        encoding="utf-8",
    )
    cfg = load_config(cfg_file)
    assert len(cfg.suite_e2e()) == 1
    c = cfg.suite_e2e()[0]
    assert c["name"] == "del2d1"
    assert c["tol_atol"] == 1e-12
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_config.py -v`
Expected: FAIL（`ModuleNotFoundError: framework.tolerance`）

- [ ] **Step 3: 实现 tolerance.py**

创建 `test/framework/tolerance.py`：
```python
from dataclasses import dataclass


@dataclass(frozen=True)
class Tolerance:
    """数值对比容差：|actual-baseline| <= atol + rtol*|baseline|"""
    atol: float
    rtol: float = 0.0

    def accept(self, actual: float, baseline: float) -> bool:
        return abs(actual - baseline) <= self.atol + self.rtol * abs(baseline)
```

- [ ] **Step 4: 实现 config.py**

创建 `test/framework/config.py`：
```python
"""读取 config.toml，提供结构化访问。Python 3.11+ 用标准库 tomllib。"""
import sys
from dataclasses import dataclass
from pathlib import Path

if sys.version_info >= (3, 11):
    import tomllib
else:  # pragma: no cover
    import tomli as tomllib  # 需 pip install tomli


@dataclass
class Toolchain:
    cmake_generator: str
    source_dir: str
    build_dir: str
    output_subdir: str


class Config:
    def __init__(self, raw: dict, base_dir: Path):
        self._raw = raw
        self.base_dir = base_dir  # config.toml 所在目录，用于解析相对路径
        tc = raw.get("toolchain", {})
        self.toolchain = Toolchain(
            cmake_generator=tc.get("cmake_generator", "Unix Makefiles"),
            source_dir=tc.get("source_dir", "FEMproject"),
            build_dir=tc.get("build_dir", "test/build"),
            output_subdir=tc.get("output_subdir", "output"),
        )

    def suite_e2e(self) -> list[dict]:
        return self._raw.get("suite", {}).get("e2e", {}).get("cases", [])

    def suite_unit(self) -> list[dict]:
        return self._raw.get("suite", {}).get("unit", {}).get("cases", [])

    def suite_generator(self) -> dict:
        return self._raw.get("suite", {}).get("generator", {})

    def suite_analytical(self) -> list[dict]:
        return self._raw.get("suite", {}).get("analytical", {}).get("cases", [])


def load_config(path) -> Config:
    path = Path(path)
    with open(path, "rb") as f:
        raw = tomllib.load(f)
    return Config(raw, base_dir=path.parent)
```

- [ ] **Step 5: 写真实 config.toml**

创建 `test/config.toml`：
```toml
[toolchain]
cmake_generator = "Unix Makefiles"
source_dir = "FEMproject"
build_dir = "test/build"
output_subdir = "output"

[suite.e2e]
cases = [
  { name = "del2d1", target = "del2d", project = "del2d",
    case_dir = "models/del2d1.gid", baseline = "del2d.post.res",
    output = "del2d.post.res", tol_atol = 1e-12, tol_rtol = 0.0 },
]

[suite.unit]
cases = []

[suite.generator]
pytest_dir = "suites/generator"
default_in_all = false

[suite.analytical]
cases = []
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_config.py -v`
Expected: 3 passed

- [ ] **Step 7: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/tolerance.py test/framework/config.py test/config.toml test/framework/tests/test_config.py
git commit -m "test: 实现 tolerance 与 config.toml 读取"
```

---

### Task 3: parser.py（GiD .post.res 解析）

**Files:**
- Create: `test/framework/parser.py`
- Test: `test/framework/tests/test_parser.py`
- 夹具：`test/framework/tests/fixtures/del2d_sample.res`（从真实基准截取）

**Interfaces:**
- Produces: `ResBlock(result_name, analysis, step, result_type, location, components, values)`；`parse_res_file(path) -> dict[(result_name:str, step:int), ResBlock]`。

- [ ] **Step 1: 准备测试夹具（从真实基准截取）**

从 `test/models/del2d1.gid/del2d.post.res` 截取前 2 步的 disp 段 + 1 个 stress 段，存为 `test/framework/tests/fixtures/del2d_sample.res`。内容（节选，节点号压缩到前 3 个）：
```
GID Post Results File 1.0
Result "disp" "Load Analysis"           1 Vector OnNodes
ComponentNames "u" "v" 
Values
         1                -1.0214411e-05 -9.7636690e-06
         2                -8.3579323e-06 -9.7639201e-06
         3                -1.0214667e-05 -7.8254008e-06
End Values
Result "stress" "Load Analysis"           1 Matrix OnNodes
ComponentNames "sigmaXX" "sigmaYY" "sigmaXY" 
Values
         1                 1.2345678e+03 -2.3456789e+03  3.4567890e+02
End Values
Result "disp" "Load Analysis"           2 Vector OnNodes
ComponentNames "u" "v" 
Values
         1                -2.0214411e-05 -9.7636690e-06
End Values
```
（执行者：用真实文件前若干行替换上面的数值，确保节点号与分量数正确即可。）

- [ ] **Step 2: 写失败测试**

创建 `test/framework/tests/test_parser.py`：
```python
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.parser import parse_res_file, ResBlock

FIXTURE = Path(__file__).parent / "fixtures" / "del2d_sample.res"


def test_parse_disp_block_step1():
    blocks = parse_res_file(FIXTURE)
    assert ("disp", 1) in blocks
    b = blocks[("disp", 1)]
    assert b.result_name == "disp"
    assert b.step == 1
    assert b.result_type == "Vector"
    assert b.location == "OnNodes"
    assert b.components == ["u", "v"]
    assert b.values[1] == [-1.0214411e-05, -9.7636690e-06]


def test_parse_stress_matrix_components():
    blocks = parse_res_file(FIXTURE)
    b = blocks[("stress", 1)]
    assert b.components == ["sigmaXX", "sigmaYY", "sigmaXY"]
    assert b.result_type == "Matrix"
    assert len(b.values[1]) == 3


def test_parse_multiple_steps():
    blocks = parse_res_file(FIXTURE)
    assert ("disp", 1) in blocks
    assert ("disp", 2) in blocks
    assert blocks[("disp", 2)].values[1][0] == -2.0214411e-05


def test_parse_node_keys_are_int():
    blocks = parse_res_file(FIXTURE)
    for node_id in blocks[("disp", 1)].values:
        assert isinstance(node_id, int)
```

- [ ] **Step 3: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_parser.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 4: 实现 parser.py**

创建 `test/framework/parser.py`：
```python
"""GiD .post.res 结果文件解析。

格式（权威：GiD 17 Customization Manual）：
  Result "<名>" "<分析>" <步> <类型> <位置>
  ComponentNames "<c1>" "<c2>" ...
  Values
      <实体号> <v1> <v2> ...
  End Values
"""
import re
from dataclasses import dataclass, field
from pathlib import Path

RESULT_RE = re.compile(r'^Result\s+"([^"]+)"\s+"([^"]+)"\s+(\d+)\s+(\S+)\s+(\S+)')
COMPONENT_RE = re.compile(r'"([^"]*)"')


@dataclass
class ResBlock:
    result_name: str
    analysis: str
    step: int
    result_type: str
    location: str
    components: list[str] = field(default_factory=list)
    values: dict[int, list[float]] = field(default_factory=dict)


def parse_res_file(path) -> dict[tuple[str, int], ResBlock]:
    """解析 .res → {(result_name, step): ResBlock}。"""
    blocks: dict[tuple[str, int], ResBlock] = {}
    cur: ResBlock | None = None
    in_values = False

    with open(path, "r", encoding="utf-8", errors="replace") as f:
        for line in f:
            line = line.rstrip("\n")
            m = RESULT_RE.match(line)
            if m:
                cur = ResBlock(
                    result_name=m.group(1), analysis=m.group(2),
                    step=int(m.group(3)), result_type=m.group(4), location=m.group(5),
                )
                blocks[(cur.result_name, cur.step)] = cur
                in_values = False
                continue
            if line.startswith("ComponentNames") and cur is not None:
                cur.components = COMPONENT_RE.findall(line)
                continue
            if line.strip() == "Values":
                in_values = True
                continue
            if line.strip() == "End Values":
                in_values = False
                continue
            if in_values and cur is not None:
                parts = line.split()
                if len(parts) < 2:
                    continue
                try:
                    node_id = int(parts[0])
                    cur.values[node_id] = [float(x) for x in parts[1:]]
                except ValueError:
                    continue  # 跳过无法解析行
    return blocks
```

- [ ] **Step 5: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_parser.py -v`
Expected: 4 passed

- [ ] **Step 6: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/parser.py test/framework/tests/test_parser.py test/framework/tests/fixtures/del2d_sample.res
git commit -m "test: 实现 GiD .post.res 解析器"
```

---

### Task 4: comparator.py（结构一致性 + 数值对比）

**Files:**
- Create: `test/framework/comparator.py`
- Test: `test/framework/tests/test_comparator.py`

**Interfaces:**
- Consumes: `parse_res_file`（Task 3）、`Tolerance`（Task 2）
- Produces: `CompareResult(structural_ok, structural_errors, max_abs_err, n_points, n_over_tol, first_over, worst_point, worst_delta, passed)`；`compare(actual_blocks, baseline_blocks, tol) -> CompareResult`。

- [ ] **Step 1: 写失败测试**

创建 `test/framework/tests/test_comparator.py`：
```python
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.parser import parse_res_file, ResBlock
from framework.tolerance import Tolerance
from framework.comparator import compare

FIXTURE = Path(__file__).parent / "fixtures" / "del2d_sample.res"


def _blocks():
    return parse_res_file(FIXTURE)


def test_identical_blocks_pass():
    b = _blocks()
    r = compare(b, b, Tolerance(atol=1e-12))
    assert r.structural_ok
    assert r.passed
    assert r.n_over_tol == 0
    assert r.max_abs_err == 0.0


def test_numeric_drift_detected():
    actual = _blocks()
    # 篡改 disp step1 node1 的 u 分量
    actual[("disp", 1)].values[1][0] += 1e-5
    r = compare(actual, _blocks(), Tolerance(atol=1e-12))
    assert r.structural_ok
    assert not r.passed
    assert r.n_over_tol >= 1
    assert "disp" in r.first_over
    assert "1" in r.first_over  # step 或 node


def test_structural_drift_step_missing():
    actual = _blocks()
    del actual[("disp", 2)]
    r = compare(actual, _blocks(), Tolerance(atol=1e-12))
    assert not r.structural_ok
    assert not r.passed
    assert any("disp" in e for e in r.structural_errors)


def test_structural_drift_component_change():
    actual = _blocks()
    actual[("disp", 1)].components = ["u", "v", "w"]
    r = compare(actual, _blocks(), Tolerance(atol=1e-12))
    assert not r.structural_ok


def test_worst_point_tracked():
    actual = _blocks()
    actual[("disp", 1)].values[1][0] += 1e-3
    actual[("disp", 1)].values[2][0] += 1e-2  # 更大
    r = compare(actual, _blocks(), Tolerance(atol=1e-12))
    assert abs(r.worst_delta - 1e-2) < 1e-13
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_comparator.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 comparator.py**

创建 `test/framework/comparator.py`：
```python
"""两段判定：结构一致性（硬信号）优先于数值对比。"""
from dataclasses import dataclass, field

from framework.tolerance import Tolerance

Blocks = dict  # dict[(result_name, step), ResBlock]


@dataclass
class CompareResult:
    structural_ok: bool
    structural_errors: list[str] = field(default_factory=list)
    max_abs_err: float = 0.0
    n_points: int = 0
    n_over_tol: int = 0
    first_over: str | None = None   # "result=disp step=1 node=42 comp=v"
    worst_point: str | None = None
    worst_delta: float = 0.0
    passed: bool = True


def _check_structure(actual: Blocks, baseline: Blocks) -> list[str]:
    """返回结构漂移描述列表；空列表表示结构一致。"""
    errors: list[str] = []
    a_keys, b_keys = set(actual.keys()), set(baseline.keys())
    if a_keys != b_keys:
        missing = b_keys - a_keys
        extra = a_keys - b_keys
        if missing:
            errors.append(f"缺失结果段: {sorted(missing)}")
        if extra:
            errors.append(f"新增结果段: {sorted(extra)}")
        return errors  # 键都不一致，分量/节点检查无意义
    for key in b_keys:
        a, b = actual[key], baseline[key]
        if a.components != b.components:
            errors.append(f"分量变化 [{key}]: actual={a.components} baseline={b.components}")
        if set(a.values.keys()) != set(b.values.keys()):
            errors.append(
                f"节点集合变化 [{key}]: actual 缺 {sorted(set(b.values)-set(a.values))}, "
                f"多 {sorted(set(a.values)-set(b.values))}"
            )
    return errors


def _compare_numeric(actual: Blocks, baseline: Blocks, tol: Tolerance) -> CompareResult:
    r = CompareResult(structural_ok=True)
    for key, b_blk in baseline.items():
        a_blk = actual[key]
        for node_id, b_vals in b_blk.items():
            a_vals = a_blk.values[node_id]
            for i, comp in enumerate(b_blk.components):
                a_v, b_v = a_vals[i], b_vals[i]
                delta = abs(a_v - b_v)
                r.n_points += 1
                if delta > r.worst_delta:
                    r.worst_delta = delta
                    r.worst_point = f"result={key[0]} step={key[1]} node={node_id} comp={comp}"
                if delta > r.max_abs_err:
                    r.max_abs_err = delta
                if not tol.accept(a_v, b_v):
                    r.n_over_tol += 1
                    if r.first_over is None:
                        r.first_over = f"result={key[0]} step={key[1]} node={node_id} comp={comp}"
    r.passed = (r.n_over_tol == 0)
    return r


def compare(actual: Blocks, baseline: Blocks, tol: Tolerance) -> CompareResult:
    structural_errors = _check_structure(actual, baseline)
    if structural_errors:
        return CompareResult(structural_ok=False, structural_errors=structural_errors, passed=False)
    return _compare_numeric(actual, baseline, tol)
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_comparator.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/comparator.py test/framework/tests/test_comparator.py
git commit -m "test: 实现两段判定对比引擎（结构优先+数值）"
```

---

### Task 5: builder.py（cmake configure + build，target 缓存）

**Files:**
- Create: `test/framework/builder.py`
- Test: `test/framework/tests/test_builder.py`

**Interfaces:**
- Produces: `Builder(source_dir, build_dir, generator="Unix Makefiles", extra_cmake_args=None)`；`.build(targets: list[str]) -> dict[str, Path]`（target→exe 路径）；`.force_reconfigure()`。

- [ ] **Step 1: 写失败测试（mock subprocess）**

创建 `test/framework/tests/test_builder.py`：
```python
from pathlib import Path
import sys
from unittest.mock import patch, call
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.builder import Builder


def test_build_caches_configured(tmp_path):
    b = Builder(source_dir="FEMproject", build_dir=str(tmp_path / "build"))
    with patch("framework.builder.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 0
        b.build(["del2d"])
        b.build(["del2d"])  # 第二次应只 build 不重复 configure
        cmds = [c.args[0] for c in mock_run.call_args_list]
        # configure 一次 + build 一次（第二次被 target 缓存跳过）
        assert sum("cmake" in c and "-B" in c for c in cmds) >= 1
        assert sum("--build" in c for c in cmds) == 1


def test_build_returns_exe_path(tmp_path):
    b = Builder(source_dir="FEMproject", build_dir=str(tmp_path / "build"))
    with patch("framework.builder.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 0
        exes = b.build(["del2d"])
        assert "del2d" in exes
        assert exes["del2d"].name.startswith("del2d")


def test_build_new_target_only_builds_missing(tmp_path):
    b = Builder(source_dir="FEMproject", build_dir=str(tmp_path / "build"))
    with patch("framework.builder.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 0
        b.build(["del2d"])
        b.build(["hel2d"])
        # 第二次只 build hel2d（del2d 已缓存）
        second = mock_run.call_args_list[-1].args[0]
        assert "hel2d" in second
        assert "del2d" not in second


def test_build_failure_raises(tmp_path):
    import pytest
    b = Builder(source_dir="FEMproject", build_dir=str(tmp_path / "build"))
    with patch("framework.builder.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 1
        with pytest.raises(RuntimeError):
            b.build(["del2d"])
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_builder.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 builder.py**

创建 `test/framework/builder.py`：
```python
"""cmake configure + build 封装。target 级缓存避免重复构建。"""
import subprocess
import sys
from pathlib import Path


class BuildError(RuntimeError):
    pass


class Builder:
    def __init__(self, source_dir, build_dir, generator="Unix Makefiles",
                 extra_cmake_args=None, output_subdir="output"):
        self.source_dir = str(source_dir)
        self.build_dir = str(build_dir)
        self.generator = generator
        self.extra_cmake_args = extra_cmake_args or []
        self.output_subdir = output_subdir
        self._configured = False
        self._built_targets: set[str] = set()

    def _configure(self):
        Path(self.build_dir).mkdir(parents=True, exist_ok=True)
        cmd = ["cmake", "-B", self.build_dir, "-S", self.source_dir,
               "-G", self.generator] + self.extra_cmake_args
        r = subprocess.run(cmd, capture_output=True, text=True)
        if r.returncode != 0:
            raise BuildError(f"cmake configure 失败:\n{r.stdout}\n{r.stderr}")
        self._configured = True

    def _exe_path(self, target):
        # cygwin/Windows 下可执行带 .exe；Linux 无后缀
        exe = Path(self.build_dir) / self.output_subdir / target
        if sys.platform == "cygwin" or sys.platform.startswith("win"):
            exe = exe.with_suffix(".exe")
        return exe

    def build(self, targets):
        if not self._configured:
            self._configure()
        new = [t for t in targets if t not in self._built_targets]
        if new:
            cmd = ["cmake", "--build", self.build_dir, "--target", *new, "-j"]
            r = subprocess.run(cmd, capture_output=True, text=True)
            if r.returncode != 0:
                raise BuildError(f"cmake build 失败 ({new}):\n{r.stdout}\n{r.stderr}")
            self._built_targets.update(new)
        return {t: self._exe_path(t) for t in targets}

    def force_reconfigure(self):
        self._configured = False
        self._built_targets.clear()
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_builder.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/builder.py test/framework/tests/test_builder.py
git commit -m "test: 实现 builder（cmake 封装+target 缓存）"
```

---

### Task 6: runner.py（subprocess 跑 exe + 收集产出）

**Files:**
- Create: `test/framework/runner.py`
- Test: `test/framework/tests/test_runner.py`

**Interfaces:**
- Produces: `RunResult(returncode, stdout, stderr, outputs: dict[str,Path], timed_out)`；`run(exe, args, cwd, expect_outputs, timeout=600) -> RunResult`。

- [ ] **Step 1: 写失败测试（mock subprocess + 真实临时文件）**

创建 `test/framework/tests/test_runner.py`：
```python
from pathlib import Path
import sys
from unittest.mock import patch
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.runner import run


def test_run_collects_existing_output(tmp_path):
    # 模拟 exe 成功 + 产出文件存在
    out_file = tmp_path / "del2d.post.res"
    out_file.write_text("dummy", encoding="utf-8")
    with patch("framework.runner.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 0
        mock_run.return_value.stdout = ""
        mock_run.return_value.stderr = ""
        r = run("del2d.exe", ["del2d", "."], tmp_path, ["del2d.post.res"])
        assert r.returncode == 0
        assert "del2d.post.res" in r.outputs
        assert r.outputs["del2d.post.res"].exists()


def test_run_missing_output_not_collected(tmp_path):
    with patch("framework.runner.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 0
        mock_run.return_value.stdout = ""
        mock_run.return_value.stderr = ""
        r = run("del2d.exe", ["del2d", "."], tmp_path, ["del2d.post.res"])
        assert "del2d.post.res" not in r.outputs


def test_run_nonzero_returncode(tmp_path):
    with patch("framework.runner.subprocess.run") as mock_run:
        mock_run.return_value.returncode = 139  # segfault
        mock_run.return_value.stdout = ""
        mock_run.return_value.stderr = "Segmentation fault"
        r = run("del2d.exe", ["del2d", "."], tmp_path, [])
        assert r.returncode == 139
        assert "Segmentation" in r.stderr
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_runner.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 runner.py**

创建 `test/framework/runner.py`：
```python
"""运行可执行程序并收集产出文件。"""
import subprocess
from dataclasses import dataclass, field
from pathlib import Path


@dataclass
class RunResult:
    returncode: int
    stdout: str
    stderr: str
    outputs: dict = field(default_factory=dict)  # name -> Path（仅含存在的）
    timed_out: bool = False


def run(exe, args, cwd, expect_outputs, timeout=600) -> RunResult:
    exe = str(exe)
    args = [str(a) for a in args]
    cwd = Path(cwd)
    try:
        proc = subprocess.run(
            [exe, *args], cwd=str(cwd), capture_output=True,
            text=True, timeout=timeout,
        )
    except subprocess.TimeoutExpired as e:
        return RunResult(returncode=-1, stdout=e.stdout or "", stderr=e.stderr or "",
                         timed_out=True)

    outputs = {}
    for name in expect_outputs:
        p = cwd / name
        if p.exists():
            outputs[name] = p
    return RunResult(returncode=proc.returncode, stdout=proc.stdout,
                     stderr=proc.stderr, outputs=outputs)
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_runner.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/runner.py test/framework/tests/test_runner.py
git commit -m "test: 实现 runner（subprocess+产出收集）"
```

---

### Task 7: report.py（聚合 + 终端报告 + 退出码）

**Files:**
- Create: `test/framework/report.py`
- Test: `test/framework/tests/test_report.py`

**Interfaces:**
- Consumes: `CaseResult`（Task 8 定义，此处先定义最小结构）
- Produces: `aggregate(results: list[CaseResult]) -> int`（退出码 0/1/2）；`print_report(results)`。

**注：** `CaseResult` 在 Task 8 的 `case.py` 定义。为避免循环依赖，`CaseResult` 定义在 `case.py`，`report.py` 从 `case` 导入。本任务先在 `report.py` 用 `from framework.case import CaseResult`，故需先建 `case.py` 的 `CaseResult` 部分。调整：本任务**同时**建立 `case.py` 的 `CaseResult` 数据类（仅数据类，不含 Case 实现与各套件类，那些在 Task 8）。

- [ ] **Step 1: 先建 case.py 的 CaseResult 数据类**

创建 `test/framework/case.py`（本步只放数据类，Task 8 补充 Case 协议与套件类）：
```python
"""Case 抽象与 CaseResult。框架只依赖 Case 协议，套件是其实现。"""
from dataclasses import dataclass, field


@dataclass
class CaseResult:
    name: str          # "e2e.del2d1"
    suite: str         # "e2e" | "unit" | "generator" | "analytical"
    status: str        # "pass" | "fail" | "error" | "skip"
    metric: dict = field(default_factory=dict)
    detail: str = ""
```

- [ ] **Step 2: 写失败测试**

创建 `test/framework/tests/test_report.py`：
```python
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.case import CaseResult
from framework.report import aggregate, EXIT_OK, EXIT_FAIL, EXIT_ERROR


def _r(name, status):
    return CaseResult(name=name, suite="e2e", status=status)


def test_all_pass_returns_0():
    assert aggregate([_r("a", "pass"), _r("b", "pass")]) == EXIT_OK


def test_any_fail_returns_1():
    assert aggregate([_r("a", "pass"), _r("b", "fail")]) == EXIT_FAIL


def test_error_without_fail_returns_2():
    assert aggregate([_r("a", "error"), _r("b", "pass")]) == EXIT_ERROR


def test_fail_takes_precedence_over_error():
    assert aggregate([_r("a", "fail"), _r("b", "error")]) == EXIT_FAIL


def test_skip_ignored():
    assert aggregate([_r("a", "skip")]) == EXIT_OK
```

- [ ] **Step 3: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_report.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 4: 实现 report.py**

创建 `test/framework/report.py`：
```python
"""聚合 CaseResult，打印报告，返回退出码。"""
from framework.case import CaseResult

EXIT_OK = 0
EXIT_FAIL = 1     # 有真实回归（fail）
EXIT_ERROR = 2    # 框架/基础设施问题（error）


def aggregate(results: list[CaseResult]) -> int:
    print_report(results)
    has_fail = any(r.status == "fail" for r in results)
    has_error = any(r.status == "error" for r in results)
    if has_fail:
        return EXIT_FAIL
    if has_error:
        return EXIT_ERROR
    return EXIT_OK


def print_report(results: list[CaseResult]):
    print("\n" + "=" * 70)
    print(f"{'CASE':<28} {'STATUS':<8} {'METRIC'}")
    print("-" * 70)
    for r in results:
        metric_str = ""
        if r.status == "pass" and r.metric.get("max_abs_err") is not None:
            metric_str = f"max|Δ|={r.metric['max_abs_err']:.2e}"
        elif r.status == "fail":
            metric_str = r.detail[:40]
        elif r.status == "error":
            metric_str = r.detail[:40]
        print(f"{r.name:<28} {r.status:<8} {metric_str}")
    print("=" * 70)
    n_pass = sum(1 for r in results if r.status == "pass")
    n_fail = sum(1 for r in results if r.status == "fail")
    n_err = sum(1 for r in results if r.status == "error")
    n_skip = sum(1 for r in results if r.status == "skip")
    print(f"合计: {n_pass} pass / {n_fail} fail / {n_err} error / {n_skip} skip")
    # 失败详情
    for r in results:
        if r.status in ("fail", "error") and r.detail:
            print(f"\n[{r.status.upper()}] {r.name}\n      {r.detail}")
```

- [ ] **Step 5: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_report.py -v`
Expected: 5 passed

- [ ] **Step 6: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/case.py test/framework/report.py test/framework/tests/test_report.py
git commit -m "test: 实现 CaseResult 与 report 聚合退出码"
```

---

### Task 8: case.py 补全（Case 协议 + E2ECase/UnitCase/GeneratorCase/AnalyticalCase）

**Files:**
- Modify: `test/framework/case.py`（在 Task 7 的 CaseResult 基础上补全）
- Test: `test/framework/tests/test_case.py`

**Interfaces:**
- Consumes: `Builder`、`run`、`compare`、`parse_res_file`、`Tolerance`
- Produces: `Case` 协议；`E2ECase`（端到端回归，核心）；`UnitCase`/`GeneratorCase`/`AnalyticalCase`。

- [ ] **Step 1: 写失败测试（E2ECase 用 mock 全链路）**

创建 `test/framework/tests/test_case.py`：
```python
from pathlib import Path
import sys
import shutil
from unittest.mock import MagicMock
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.case import E2ECase
from framework.tolerance import Tolerance
from framework.runner import RunResult

FIXTURE = Path(__file__).parent / "fixtures" / "del2d_sample.res"


def _make_case(case_dir, tmp_path):
    builder = MagicMock()
    builder.build.return_value = {"del2d": tmp_path / "del2d.exe"}
    builder.build_dir = str(tmp_path / "build")  # _prepare_work_dir 用
    return builder, E2ECase(
        name="e2e.del2d1", target="del2d", project="del2d",
        case_dir=case_dir, baseline="del2d.post.res", output="del2d.post.res",
        tol=Tolerance(atol=1e-12), builder=builder,
    )


def _setup_case_dir(tmp_path):
    case_dir = tmp_path / "models" / "del2d1.gid"
    case_dir.mkdir(parents=True)
    shutil.copy(FIXTURE, case_dir / "del2d.post.res")  # 基准
    return case_dir


def test_e2e_pass_when_identical(tmp_path):
    case_dir = _setup_case_dir(tmp_path)
    builder, case = _make_case(case_dir, tmp_path)

    def fake_run(exe, args, cwd, expect, timeout=600):
        out = Path(cwd) / "del2d.post.res"
        shutil.copy(FIXTURE, out)  # 模拟 exe 产出 == 基准
        return RunResult(0, "", "", {"del2d.post.res": out})

    case._runner_run = fake_run
    r = case.run(None)
    assert r.status == "pass"


def test_e2e_work_dir_isolated_from_baseline(tmp_path):
    """关键：exe 在隔离 work_dir 跑，case_dir 的基准不被覆盖。"""
    case_dir = _setup_case_dir(tmp_path)
    before = (case_dir / "del2d.post.res").read_bytes()
    builder, case = _make_case(case_dir, tmp_path)

    def fake_run(exe, args, cwd, expect, timeout=600):
        out = Path(cwd) / "del2d.post.res"
        shutil.copy(FIXTURE, out)
        return RunResult(0, "", "", {"del2d.post.res": out})

    case._runner_run = fake_run
    case.run(None)
    after = (case_dir / "del2d.post.res").read_bytes()
    assert before == after  # 基准未被改动
    assert not (Path(builder.build_dir) / "run" / "e2e.del2d1" / "del2d.post.res").parent.exists() or \
           (Path(builder.build_dir) / "run" / "e2e.del2d1" / "del2d.post.res").exists()


def test_e2e_error_when_exe_crash(tmp_path):
    case_dir = _setup_case_dir(tmp_path)
    builder, case = _make_case(case_dir, tmp_path)
    case._runner_run = MagicMock(return_value=RunResult(139, "", "segfault", {}))
    r = case.run(None)
    assert r.status == "error"


def test_e2e_error_when_output_missing(tmp_path):
    case_dir = _setup_case_dir(tmp_path)
    builder, case = _make_case(case_dir, tmp_path)
    case._runner_run = MagicMock(return_value=RunResult(0, "", "", {}))  # 无产出
    r = case.run(None)
    assert r.status == "error"
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_case.py -v`
Expected: FAIL（`E2ECase` 不存在）

- [ ] **Step 3: 补全 case.py**

在 `test/framework/case.py` 追加（保留 Task 7 的 CaseResult）：
```python
from pathlib import Path
import shutil
from typing import Protocol

from framework.parser import parse_res_file
from framework.comparator import compare
from framework.runner import run as _runner_run_default, RunResult
from framework.tolerance import Tolerance


class Case(Protocol):
    name: str
    suite: str
    def run(self, ctx) -> "CaseResult": ...


class E2ECase:
    """端到端回归：构建示例 → 在隔离工作目录跑 → 对比基准（基准永不被 exe 覆盖）。"""

    suite = "e2e"

    def __init__(self, name, target, project, case_dir, baseline, output,
                 tol: Tolerance, builder, timeout=600):
        self.name = name
        self.target = target
        self.project = project
        self.case_dir = Path(case_dir)
        self.baseline = baseline
        self.output = output
        self.tol = tol
        self.builder = builder
        self.timeout = timeout
        self._runner_run = _runner_run_default  # 可被测试替换

    def _prepare_work_dir(self) -> Path:
        """隔离工作目录：拷贝输入（排除 .post.res 基准与 .bak），避免 exe 覆盖基准。"""
        work_dir = Path(self.builder.build_dir) / "run" / self.name
        if work_dir.exists():
            shutil.rmtree(work_dir)
        work_dir.mkdir(parents=True)
        for f in self.case_dir.iterdir():
            if f.is_file() and ".post.res" not in f.name and not f.name.endswith(".bak"):
                shutil.copy2(f, work_dir / f.name)
        return work_dir

    def run(self, ctx) -> CaseResult:
        try:
            exes = self.builder.build([self.target])
        except Exception as e:
            return CaseResult(self.name, self.suite, "error", detail=f"构建失败: {e}")
        work_dir = self._prepare_work_dir()
        rr: RunResult = self._runner_run(exes[self.target], [self.project, "."], work_dir,
                                         [self.output], timeout=self.timeout)
        if rr.timed_out:
            return CaseResult(self.name, self.suite, "error", detail=f"运行超时({self.timeout}s)")
        if rr.returncode != 0:
            return CaseResult(self.name, self.suite, "error",
                              detail=f"退出码 {rr.returncode}: {rr.stderr[:200]}")
        if self.output not in rr.outputs:
            return CaseResult(self.name, self.suite, "error",
                              detail=f"未产出 {self.output}")
        actual = parse_res_file(rr.outputs[self.output])
        baseline_path = self.case_dir / self.baseline
        if not baseline_path.exists():
            return CaseResult(self.name, self.suite, "error",
                              detail=f"基准缺失: {baseline_path}")
        baseline = parse_res_file(baseline_path)
        cr = compare(actual, baseline, self.tol)
        if not cr.structural_ok:
            return CaseResult(self.name, self.suite, "fail",
                              detail="结构漂移: " + "; ".join(cr.structural_errors))
        metric = {"max_abs_err": cr.max_abs_err, "n_points": cr.n_points, "n_over_tol": cr.n_over_tol}
        if cr.n_over_tol > 0:
            detail = (f"max|Δ|={cr.worst_delta:.2e} 超差 {cr.n_over_tol}/{cr.n_points}; "
                      f"首超 {cr.first_over}")
            return CaseResult(self.name, self.suite, "fail", metric=metric, detail=detail)
        return CaseResult(self.name, self.suite, "pass", metric=metric)


class UnitCase:
    """C++ Catch2 单测：跑 cdfeg_unit_test.exe，退出码 0=全过。"""
    suite = "unit"

    def __init__(self, name, binary, builder, timeout=300):
        self.name = name
        self.binary = binary
        self.builder = builder
        self.timeout = timeout

    def run(self, ctx) -> CaseResult:
        try:
            exes = self.builder.build([self.binary])
        except Exception as e:
            return CaseResult(self.name, self.suite, "error", detail=f"构建失败: {e}")
        rr = _runner_run_default(exes[self.binary], [], Path("."), [], timeout=self.timeout)
        if rr.returncode == 0:
            return CaseResult(self.name, self.suite, "pass",
                              detail=rr.stdout[-200:] if rr.stdout else "")
        return CaseResult(self.name, self.suite, "fail",
                          detail=f"Catch2 断言失败:\n{rr.stdout[:500]}")


class GeneratorCase:
    """pyTool 生成器测试：跑 pytest 子目录。"""
    suite = "generator"

    def __init__(self, name, pytest_dir):
        self.name = name
        self.pytest_dir = pytest_dir

    def run(self, ctx) -> CaseResult:
        import subprocess
        r = subprocess.run(["python", "-m", "pytest", str(self.pytest_dir), "-v"],
                           capture_output=True, text=True)
        if r.returncode == 0:
            return CaseResult(self.name, self.suite, "pass")
        return CaseResult(self.name, self.suite, "fail", detail=r.stdout[:500])


class AnalyticalCase:
    """解析解校验：跑示例后取关键点与解析公式比（首批仅占位）。"""
    suite = "analytical"

    def __init__(self, name):
        self.name = name

    def run(self, ctx) -> CaseResult:
        return CaseResult(self.name, self.suite, "skip", detail="解析解算例待实现")
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_case.py -v`
Expected: 3 passed

- [ ] **Step 5: 全框架回归**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/ -v`
Expected: 全部 passed（config/parser/comparator/builder/runner/report/case）

- [ ] **Step 6: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/case.py test/framework/tests/test_case.py
git commit -m "test: 实现 Case 抽象与 E2ECase/Unit/Generator/Analytical 套件"
```

---

## 阶段 B — 构建集成 + Tracer Bullet

### Task 9: run_tests.py CLI 入口

**Files:**
- Create: `test/run_tests.py`
- Test: 无独立单测（通过 Task 11 tracer bullet 端到端验证）

**Interfaces:**
- Consumes: 全部 framework 模块 + config
- Produces: CLI `python test/run_tests.py [--suite all|e2e|unit|generator|analytical] [--case NAME] [--rebuild] [-v] [--update NAME]`，退出码 0/1/2。

- [ ] **Step 1: 实现 run_tests.py**

创建 `test/run_tests.py`：
```python
"""CDFEG 测试系统统一入口。

用法：
  python test/run_tests.py                         # 默认 all（e2e+unit）
  python test/run_tests.py --suite e2e --case del2d1
  python test/run_tests.py --suite e2e --update del2d1   # 刷新基准（见 Task 13）
  python test/run_tests.py --suite generator       # 生成器（默认不含）
  python test/run_tests.py --rebuild -v
"""
import argparse
import sys
from pathlib import Path

TEST_DIR = Path(__file__).resolve().parent
sys.path.insert(0, str(TEST_DIR))  # 使 `import framework` 可用
PROJ_ROOT = TEST_DIR.parent

from framework.config import load_config
from framework.tolerance import Tolerance
from framework.builder import Builder
from framework.case import E2ECase, UnitCase, GeneratorCase, AnalyticalCase
from framework.report import aggregate


def build_cases(cfg, args):
    builder = Builder(
        source_dir=str(PROJ_ROOT / cfg.toolchain.source_dir),
        build_dir=str(PROJ_ROOT / cfg.toolchain.build_dir),
        generator=cfg.toolchain.cmake_generator,
        extra_cmake_args=["-DCDFEG_BUILD_TESTS=ON"],
        output_subdir=cfg.toolchain.output_subdir,
    )
    if args.rebuild:
        builder.force_reconfigure()

    cases = []
    suites = _resolve_suites(args)
    if "e2e" in suites:
        for c in cfg.suite_e2e():
            if args.case and c["name"] != args.case:
                continue
            cases.append(E2ECase(
                name=f"e2e.{c['name']}", target=c["target"], project=c["project"],
                case_dir=TEST_DIR / c["case_dir"], baseline=c["baseline"],
                output=c["output"], tol=Tolerance(c["tol_atol"], c.get("tol_rtol", 0.0)),
                builder=builder,
            ))
    if "unit" in suites:
        for c in cfg.suite_unit():
            if args.case and c["name"] != args.case:
                continue
            cases.append(UnitCase(name=f"unit.{c['name']}", binary=c["binary"], builder=builder))
    if "generator" in suites:
        g = cfg.suite_generator()
        cases.append(GeneratorCase(name="generator.pytool",
                                   pytest_dir=TEST_DIR / g.get("pytest_dir", "suites/generator")))
    if "analytical" in suites:
        for c in cfg.suite_analytical():
            cases.append(AnalyticalCase(name=f"analytical.{c['name']}"))
    return cases, builder


def _resolve_suites(args):
    if args.suite == "all":
        return ["e2e", "unit"]  # generator 默认不含
    return [args.suite]


def main():
    ap = argparse.ArgumentParser(description="CDFEG 测试系统")
    ap.add_argument("--suite", default="all", choices=["all", "e2e", "unit", "generator", "analytical"])
    ap.add_argument("--case", default=None, help="只跑指定用例名")
    ap.add_argument("--rebuild", action="store_true", help="强制重新 cmake configure")
    ap.add_argument("-v", "--verbose", action="store_true")
    ap.add_argument("--update", default=None, help="刷新指定 e2e 用例基准（Task 13）")
    args = ap.parse_args()

    if args.update:
        from framework.update import update_baseline  # Task 13 实现
        cfg = load_config(TEST_DIR / "config.toml")
        return update_baseline(cfg, args.update, PROJ_ROOT)

    cfg = load_config(TEST_DIR / "config.toml")
    cases, _ = build_cases(cfg, args)
    if not cases:
        print("无匹配用例")
        return 0
    results = [c.run(None) for c in cases]
    return aggregate(results)


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 验证 --help 可用（不触发构建）**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --help`
Expected: 打印用法说明，无异常

- [ ] **Step 3: Commit**

```bash
cd E:/myProject/CDFEG
git add test/run_tests.py
git commit -m "test: 实现 run_tests.py 统一 CLI 入口"
```

---

### Task 10: 构建集成（根 CMake option + unit CMakeLists 骨架）

**Files:**
- Modify: `FEMproject/CMakeLists.txt`
- Create: `test/suites/unit/CMakeLists.txt`

**Interfaces:**
- Produces: `CDFEG_BUILD_TESTS` option；unit target `cdfeg_unit_test`（本任务先空源，Task 15 补 Catch2 源）。

- [ ] **Step 1: 根 CMakeLists 加 option**

在 `FEMproject/CMakeLists.txt` 末尾（所有 `add_subdirectory(sample/...)` 之后）追加：
```cmake

# 测试系统（CDFEG_BUILD_TESTS=ON 时启用，默认 OFF 不干扰日常构建）
option(CDFEG_BUILD_TESTS "构建 CDFEG 测试系统（unit 单测 target）" OFF)
if(CDFEG_BUILD_TESTS)
    add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/../test/suites/unit
                     ${CMAKE_BINARY_DIR}/cdfeg_unit_test_build)
endif()
```

- [ ] **Step 2: 建 unit CMakeLists 骨架（先占位 main，Task 15 替换为 Catch2）**

创建 `test/suites/unit/CMakeLists.txt`：
```cmake
cmake_minimum_required(VERSION 3.10)
project(cdfeg_unit_test LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 14)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
if(MSVC)
    add_compile_options(/utf-8)
endif()

# Task 15 将替换 SOURCES 为 catch_main.cpp + test_*.cpp
set(SOURCES
    unit_main_placeholder.cpp
)

add_executable(cdfeg_unit_test ${SOURCES})
target_link_libraries(cdfeg_unit_test PRIVATE CDFEG)
target_include_directories(cdfeg_unit_test PRIVATE
    ${CMAKE_SOURCE_DIR}
    ${EIGEN_INCLUDE_DIR}
)
```

- [ ] **Step 3: 占位源（Task 15 替换）**

创建 `test/suites/unit/unit_main_placeholder.cpp`：
```cpp
// 占位 main，Task 15 替换为 Catch2 入口
int main() { return 0; }
```

- [ ] **Step 4: 验证根 CMake 配置开关可用（只 configure，不 build）**

Run: `cd E:/myProject/CDFEG && cmake -B test/build_cfgtest -S FEMproject -G "Unix Makefiles" -DCDFEG_BUILD_TESTS=ON`
Expected: configure 成功，输出含 `cdfeg_unit_test`（若失败检查 `add_subdirectory` 路径）

- [ ] **Step 5: 清理临时 configure 目录**

Run: `cd E:/myProject/CDFEG && rm -rf test/build_cfgtest`

- [ ] **Step 6: Commit**

```bash
cd E:/myProject/CDFEG
git add FEMproject/CMakeLists.txt test/suites/unit/CMakeLists.txt test/suites/unit/unit_main_placeholder.cpp
git commit -m "build: 根 CMake 加 CDFEG_BUILD_TESTS option 与 unit target 骨架"
```

---

### Task 11: Tracer Bullet — del2d1 e2e 端到端跑通

**Files:**
- Test: 端到端（无新文件）

**目标：** 证明架构可行——一条命令完成"构建 del2d → 跑 → 对比基准 → PASS"。

- [ ] **Step 1: 确认基准就位**

Run: `cd E:/myProject/CDFEG && ls -la test/models/del2d1.gid/`
Expected: 存在 `del2d.dat`（输入）与 `del2d.post.res`（基准）。若 `del2d.post.res` 是当前 CDFEG 输出（非旧 C newmark），记录其来源。

- [ ] **Step 2: 跑 tracer bullet**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e --case del2d1 -v`
Expected:
- builder 调 cmake configure + build `del2d` target（首次较慢）
- runner 跑 `del2d.exe del2d .` 在 `test/models/del2d1.gid/`
- comparator 对比产出 `del2d.post.res` 与基准
- 报告：`e2e.del2d1 pass max|Δ|≈0`（同库同工具链重跑应 <1e-12）
- 退出码 0

- [ ] **Step 3: 若产出文件名/参数不符，核对 sample main 入口**

若 runner 报"未产出 del2d.post.res"或 exe 报错，检查 `FEMproject/sample/DEl2D/main.cpp` 的 `setFilePath`/参数约定，确认 `project="del2d"`、`output="del2d.post.res"` 与实际一致。修正 `test/config.toml` 对应字段。

- [ ] **Step 4: 若 max|Δ| 非 ~0，排查浮点/基准来源**

同库同工具链重跑应近位级一致。若 max|Δ| > 1e-8：检查基准 `.post.res` 是否来自相同工具链（cygwin），若基准来自 MSVC 构建则末位会抖动——此时将容差放宽到 1e-8（跨构建档）或在 cygwin 下重新冻结基准（见 Task 13）。

- [ ] **Step 5: 确认 tracer bullet 稳定 PASS 后提交（如有 config 修正）**

Run: `cd E:/myProject/CDFEG && git status`
若有 `test/config.toml` 修正：
```bash
git add test/config.toml
git commit -m "test: 修正 del2d1 用例参数，tracer bullet 跑通"
```

---

## 阶段 C — 回归核心能力

### Task 12: 结构漂移检测验证（人为制造回归测捕获）

**Files:**
- Test: 端到端验证（无新文件）

**目标：** 证明 comparator 能捕获真实的结构回归（rules 点名的 nStep 差 1 陷阱）。

- [ ] **Step 1: 备份基准**

Run: `cd E:/myProject/CDFEG && cp test/models/del2d1.gid/del2d.post.res test/models/del2d1.gid/del2d.post.res.bak`

- [ ] **Step 2: 人为制造步数漂移（删掉最后一个 step 的所有段）**

编辑 `test/models/del2d1.gid/del2d.post.res`：删除最后一个步号（如 step 11）的所有 `Result..."disp/velocity/acceleration/stress"...11` 段（到对应 `End Values`）。

- [ ] **Step 3: 跑测试，确认结构漂移被捕获**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e --case del2d1`
Expected: `FAIL e2e.del2d1 [结构漂移: 缺失结果段: [('disp',11),('velocity',11),...]]`，退出码 1

- [ ] **Step 4: 恢复基准**

Run: `cd E:/myProject/CDFEG && mv test/models/del2d1.gid/del2d.post.res.bak test/models/del2d1.gid/del2d.post.res`

- [ ] **Step 5: 确认恢复正常 PASS**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e --case del2d1`
Expected: `pass`，退出码 0

- [ ] **Step 6: 无文件改动，无需 commit（验证性任务）**

记录验证结果到提交说明（可选）：
```bash
git commit --allow-empty -m "test: 验证结构漂移检测捕获 nStep 回归"
```

---

### Task 13: --update 基准更新命令 + 安全护栏

**Files:**
- Create: `test/framework/update.py`
- Modify: `test/run_tests.py`（已在 Task 9 预留 `--update` 分支，import 本模块）
- Test: `test/framework/tests/test_update.py`

**Interfaces:**
- Produces: `update_baseline(cfg, case_name, proj_root) -> int`；禁用 `all`、强制 diff 确认。

- [ ] **Step 1: 写失败测试**

创建 `test/framework/tests/test_update.py`：
```python
from pathlib import Path
import sys
import pytest
sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
from framework.update import update_baseline, UpdateError


def test_update_all_rejected(tmp_path):
    with pytest.raises(UpdateError, match="禁止"):
        # 构造最小 cfg，case_name="all" 应被拒
        from framework.config import Config
        cfg = Config({}, base_dir=tmp_path)
        update_baseline(cfg, "all", tmp_path)


def test_update_unknown_case_rejected(tmp_path):
    from framework.config import Config
    cfg = Config({}, base_dir=tmp_path)
    with pytest.raises(UpdateError):
        update_baseline(cfg, "nonexistent", tmp_path)
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_update.py -v`
Expected: FAIL（`ModuleNotFoundError`）

- [ ] **Step 3: 实现 update.py**

创建 `test/framework/update.py`：
```python
"""基准更新：显式、逐用例、强制 diff 确认。禁用 --update all。
在隔离工作目录跑示例，确认后才覆盖 case_dir 中的基准（基准永不在跑测时被触碰）。"""
import shutil
import sys
from pathlib import Path

from framework.config import Config
from framework.builder import Builder
from framework.runner import run
from framework.parser import parse_res_file
from framework.comparator import compare
from framework.tolerance import Tolerance


class UpdateError(RuntimeError):
    pass


def _find_e2e_case(cfg: Config, case_name: str) -> dict:
    for c in cfg.suite_e2e():
        if c["name"] == case_name:
            return c
    raise UpdateError(f"未知 e2e 用例: {case_name}")


def _run_in_work_dir(builder, c, case_dir, build_dir) -> Path:
    """在隔离工作目录跑示例，返回产出文件路径（不触碰 case_dir 基准）。"""
    work_dir = Path(build_dir) / "run" / f"update_{c['name']}"
    if work_dir.exists():
        shutil.rmtree(work_dir)
    work_dir.mkdir(parents=True)
    for f in case_dir.iterdir():
        if f.is_file() and ".post.res" not in f.name and not f.name.endswith(".bak"):
            shutil.copy2(f, work_dir / f.name)
    exes = builder.build([c["target"]])
    rr = run(exes[c["target"]], [c["project"], "."], work_dir, [c["output"]], timeout=600)
    if rr.returncode != 0 or c["output"] not in rr.outputs:
        raise UpdateError(f"运行失败 (code={rr.returncode}): {rr.stderr[:300]}")
    return rr.outputs[c["output"]]


def update_baseline(cfg: Config, case_name: str, proj_root: Path) -> int:
    if case_name == "all":
        raise UpdateError("禁止 --update all：必须逐用例更新，强制人工过目每个变更")
    c = _find_e2e_case(cfg, case_name)

    test_dir = proj_root / "test"
    case_dir = test_dir / c["case_dir"]
    baseline_path = case_dir / c["baseline"]
    build_dir = proj_root / cfg.toolchain.build_dir

    builder = Builder(
        source_dir=str(proj_root / cfg.toolchain.source_dir),
        build_dir=str(build_dir),
        generator=cfg.toolchain.cmake_generator,
        extra_cmake_args=["-DCDFEG_BUILD_TESTS=ON"],
        output_subdir=cfg.toolchain.output_subdir,
    )
    actual_path = _run_in_work_dir(builder, c, case_dir, build_dir)

    # diff 摘要（若旧基准存在）
    max_delta = 0.0
    if baseline_path.exists():
        cr = compare(parse_res_file(actual_path), parse_res_file(baseline_path),
                     Tolerance(atol=0.0, rtol=0.0))
        max_delta = cr.max_abs_err
        print(f"[diff] {case_name}: max|Δ|={cr.max_abs_err:.3e} "
              f"结构错误={cr.structural_errors or '无'}")
    else:
        print(f"[diff] {case_name}: 无旧基准，将新建")

    # 强制人工确认
    print(f"\n即将覆盖: {baseline_path}")
    print("输入 yes 确认更新，其他取消: ", end="", flush=True)
    answer = sys.stdin.readline().strip()
    if answer != "yes":
        print("已取消，基准未改动。")
        return 1

    shutil.copy2(actual_path, baseline_path)
    # 记录更新日志
    log = test_dir / "reports" / "update_log.txt"
    log.parent.mkdir(parents=True, exist_ok=True)
    with open(log, "a", encoding="utf-8") as f:
        f.write(f"{case_name}\tmax|Δ|={max_delta:.3e}\t(原因待填)\n")
    print(f"已更新 {baseline_path}")
    print(f"建议: git add {baseline_path} && git commit -m 'test: 刷新 {case_name} regression 基准'")
    return 0
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/myProject/CDFEG && python -m pytest test/framework/tests/test_update.py -v`
Expected: 2 passed

- [ ] **Step 5: 手动验证 --update 流程（交互）**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e --update del2d1`
Expected: 打印 diff 摘要 → 询问 yes/no → 输入非 yes → "已取消"（不破坏基准）

- [ ] **Step 6: Commit**

```bash
cd E:/myProject/CDFEG
git add test/framework/update.py test/framework/tests/test_update.py
git commit -m "test: 实现 --update 基准更新（逐用例+强制确认）"
```

---

### Task 14: 补 hel2d1 / el2d1 e2e 用例

**Files:**
- Modify: `test/config.toml`
- Create: `test/models/el2d1.gid/`（基准来自 `FEMproject/test/el2dData/el.exe` 或冻结当前 el2d 输出）

**Interfaces:**
- Consumes: 已有 Hel2D/El2D sample

- [ ] **Step 1: 确认 Hel2D 的 project/dat/output 名**

Run: `cd E:/myProject/CDFEG && grep -n "setFilePath\|argv\|project" FEMproject/sample/Hel2D/main.cpp | head`
Expected: 确认 main 第 1 参数名（`hel` 或 `hel2d`）与产出文件名（`hel.post.res`）。记录实际值。

- [ ] **Step 2: 确认 El2D 的 project/dat/output 名**

Run: `cd E:/myProject/CDFEG && grep -n "setFilePath\|argv\|project" FEMproject/sample/El2D/main.cpp | head`
记录实际 project 参数名与产出名。

- [ ] **Step 3: config.toml 追加 hel2d1 用例**

在 `test/config.toml` 的 `[[suite.e2e]] cases` 列表追加（project/output 以 Step 1 实测为准，下面假设 `hel`/`hel.post.res`）：
```toml
  { name = "hel2d1", target = "hel2d", project = "hel",
    case_dir = "models/hel2d1.gid", baseline = "hel.post.res",
    output = "hel.post.res", tol_atol = 1e-12, tol_rtol = 0.0 },
```

- [ ] **Step 4: 跑 hel2d1 首次（用现有基准）**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e --case hel2d1`
Expected: PASS（基准是当前 hel2d 输出）。若 max|Δ| 大→基准与工具链不符，用 `--update hel2d1` 重新冻结（确认 yes）。

- [ ] **Step 5: 建立 el2d1 用例与基准**

复制 `FEMproject/test/el2dData/el.gid/` 的输入到 `test/models/el2d1.gid/`：
```bash
cd E:/myProject/CDFEG
mkdir -p test/models/el2d1.gid
cp FEMproject/test/el2dData/el.gid/el.dat test/models/el2d1.gid/ 2>/dev/null || cp FEMproject/test/el2dData/el2.dat test/models/el2d1.gid/el.dat
```
首次跑 el2d 冻结基准（Step 2 确认 project 名后）：
```bash
python test/run_tests.py --suite e2e --case el2d1   # 首次预期 error（基准缺失）
# 用 accuracy 对齐 el.exe（若可用）或直接冻结当前输出
```
config.toml 追加（project/output 以 Step 2 实测为准）：
```toml
  { name = "el2d1", target = "el2d", project = "<实测>",
    case_dir = "models/el2d1.gid", baseline = "<实测>.post.res",
    output = "<实测>.post.res", tol_atol = 1e-12, tol_rtol = 0.0 },
```
冻结基准：
```bash
python test/run_tests.py --suite e2e --update el2d1   # 确认 yes
```

- [ ] **Step 6: 全 e2e 回归**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite e2e`
Expected: del2d1/hel2d1/el2d1 全 pass

- [ ] **Step 7: Commit**

```bash
cd E:/myProject/CDFEG
git add test/config.toml test/models/el2d1.gid/
git commit -m "test: 补 hel2d1 与 el2d1 e2e 用例"
```

---

## 阶段 D — 扩展套件

### Task 15: unit 套件（Catch2 + 核心库机制断言）

**Files:**
- Create: `test/suites/unit/third/catch.hpp`（下载 Catch2 v2 单头）
- Create: `test/suites/unit/catch_main.cpp`
- Create: `test/suites/unit/test_equation_system.cpp`
- Modify: `test/suites/unit/CMakeLists.txt`（替换占位源）
- Delete: `test/suites/unit/unit_main_placeholder.cpp`

**Interfaces:**
- Consumes: CDFEG 库（`EquationSystem`、`StressBL2g` 等公开接口）
- Produces: `cdfeg_unit_test` target 跑 Catch2 断言

- [ ] **Step 1: 读核心库头文件确认可测公开接口**

Run: `cd E:/myProject/CDFEG && cat FEMproject/CDFEG/EquationSystem.h`
记录 `EquationSystem` 的公开方法签名（`adda`/`applyFirstBCs`/`addFirstBC`/`solve`/`calRightVals`/`_bSavedData0` 可见性/`init` 等），确认哪些可从外部调用。**执行者必须先读此文件再写断言，签名以实际为准。**

- [ ] **Step 2: 下载 Catch2 v2 单头**

Run: `cd E:/myProject/CDFEG/test/suites/unit && mkdir -p third && curl -L -o third/catch.hpp https://github.com/catchorg/Catch2/releases/download/v2.13.10/catch.hpp`
Expected: `third/catch.hpp` 存在（约 500KB）。若网络不可达，从 Catch2 releases 手动放置 v2.13.10 单头。

- [ ] **Step 3: catch_main.cpp**

创建 `test/suites/unit/catch_main.cpp`：
```cpp
#define CATCH_CONFIG_MAIN
#include "third/catch.hpp"
```

- [ ] **Step 4: 写首批断言（签名以 Step 1 实测为准，下面是草案）**

创建 `test/suites/unit/test_equation_system.cpp`（执行者按 Step 1 实际签名调整）：
```cpp
#include "third/catch.hpp"
#include "CDFEG/EquationSystem.h"
#include <vector>
#include <set>

using namespace CDFEG;

// 断言：adda 按行主序累加（构造 2x2 已知单刚验证落位）
TEST_CASE("EquationSystem.adda 行主序累加", "[equ][adda]") {
    // 注：实际构造方式以 EquationSystem.h 公开接口为准
    // 草案意图：构造 neq=2 的系统，adda 一个 [[1,2],[3,4]] 行主序单刚，
    //          验证 _data 落位 estifn[0*2+0]=1, [0*2+1]=2, [1*2+0]=3, [1*2+1]=4
    // 执行者：依据 init()/adda() 实际签名补全构造代码
    REQUIRE(true);  // 占位，Step 1 后替换为真实断言
}

// 断言：applyFirstBCs 划行幂等（重复施加结果一致）
TEST_CASE("EquationSystem.applyFirstBCs 幂等", "[equ][bc]") {
    // 草案：施加同一一类边界两次，_data/_f 应一致
    REQUIRE(true);
}

// 断言：_bSavedData0 刷新必要性
TEST_CASE("EquationSystem._bSavedData0 刷新", "[equ][baseline]") {
    // 草案：总刚变化后不刷新 _bSavedData0 → applyFirstBCs 用旧基线 → 结果错
    REQUIRE(true);
}
```

- [ ] **Step 5: 更新 unit CMakeLists（替换占位）**

改写 `test/suites/unit/CMakeLists.txt` 的 SOURCES：
```cmake
set(SOURCES
    catch_main.cpp
    test_equation_system.cpp
)
```
并删除 `unit_main_placeholder.cpp`。

- [ ] **Step 6: 注册 unit 用例到 config**

`test/config.toml` 的 `[suite.unit]`：
```toml
[suite.unit]
cases = [ { name = "core", binary = "cdfeg_unit_test" } ]
```

- [ ] **Step 7: 构建 + 跑 unit**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite unit`
Expected: `unit.core pass`（占位断言全过）。Task 后续把 `REQUIRE(true)` 替换为真实断言（基于 Step 1 签名）。

- [ ] **Step 8: Commit**

```bash
cd E:/myProject/CDFEG
git add test/suites/unit/third/catch.hpp test/suites/unit/catch_main.cpp test/suites/unit/test_equation_system.cpp test/suites/unit/CMakeLists.txt test/config.toml
git rm test/suites/unit/unit_main_placeholder.cpp
git commit -m "test: 接入 Catch2 unit 单测套件（首批核心库断言占位）"
```

---

### Task 16: generator 套件（pyTool 解析正确性 pytest）

**Files:**
- Create: `test/suites/generator/test_parse.py`

**Interfaces:**
- Consumes: `pyTool/fepgParser`（现有）

- [ ] **Step 1: 写解析正确性测试（迁移 test_preParser 断言）**

创建 `test/suites/generator/test_parse.py`：
```python
"""pyTool 解析正确性：迁移自 pyTool/test/test_preParser.py 的断言。
需 macs 基准数据 E:/mfelProject/RegTest/testData/macs/el 存在；不存在则 skip。
"""
import os
import sys
from pathlib import Path

import pytest

PROJ_ROOT = Path(__file__).resolve().parents[3]
PYTOOL = PROJ_ROOT / "pyTool"
sys.path.insert(0, str(PYTOOL))

MACS = r"E:/mfelProject/RegTest/testData/macs"


@pytest.fixture(scope="module")
def el_project():
    if not os.path.isdir(MACS):
        pytest.skip(f"macs 基准目录不存在: {MACS}")
    from fepgParser import parseProject
    return parseProject(MACS, "el")


def test_dim_and_fields(el_project):
    assert el_project.dim == 2
    names = [f.name for f in el_project.fields]
    assert set(names) == {"ela", "elb"}


def test_ela_disp_names(el_project):
    ela = next(f for f in el_project.fields if f.name == "ela")
    assert ela.dispNames == ["u", "v"]


def test_ela_eleSubs(el_project):
    ela = next(f for f in el_project.fields if f.name == "ela")
    ele_names = [e.name for e in ela.eleSubs]
    assert "a1eq4g2" in ele_names
    assert "a2ll2" in ele_names
    a1 = next(e for e in ela.eleSubs if e.name == "a1eq4g2")
    assert a1.nNodes == 4


def test_elb_disp_names(el_project):
    elb = next(f for f in el_project.fields if f.name == "elb")
    assert elb.dispNames == ["dxx", "dyy", "dxy"]
```

- [ ] **Step 2: 跑 generator 套件（显式）**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite generator`
Expected: 若 macs 目录存在→`generator.pytool pass`；不存在→pytest skip

- [ ] **Step 3: 验证 --suite all 不含 generator**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite all 2>&1 | grep generator`
Expected: 无输出（generator 默认不含）

- [ ] **Step 4: Commit**

```bash
cd E:/myProject/CDFEG
git add test/suites/generator/test_parse.py
git commit -m "test: 接入 generator 套件（pyTool 解析正确性）"
```

---

### Task 17: analytical 接口（Case 类已就绪，注册占位）

**Files:**
- Modify: `test/config.toml`

**注：** `AnalyticalCase` 在 Task 8 已实现（返回 skip）。本任务仅注册占位，确认接口可达。具体解析解算例（悬臂梁/Laplace）后续按需补。

- [ ] **Step 1: config 注册占位**

`test/config.toml` 的 `[suite.analytical]`：
```toml
[suite.analytical]
cases = [ { name = "placeholder", skip = true } ]
```

- [ ] **Step 2: 跑 analytical（应 skip）**

Run: `cd E:/myProject/CDFEG && python test/run_tests.py --suite analytical`
Expected: `analytical.placeholder skip`，退出码 0

- [ ] **Step 3: Commit**

```bash
cd E:/myProject/CDFEG
git add test/config.toml
git commit -m "test: 注册 analytical 套件占位（接口就绪）"
```

---

## 验收

完成全部任务后，最终验收（对应设计文档 §9.2）：

- [ ] `python test/run_tests.py` 一条命令完成"构建→运行→对比→报告"全链路
- [ ] `del2d1` e2e：同库重跑 `max|Δ| < 1e-12`，PASS
- [ ] Task 12 验证：人为制造结构回归，被捕获并定位
- [ ] `--update del2d1` 走完 diff 确认流程；`--update all` 被拒
- [ ] `cdfeg_unit_test` PASS（占位断言）
- [ ] 退出码：全过 0 / 有 fail 1 / 框架 error 2

---

## Self-Review 笔记

**1. Spec 覆盖：** 设计 §3 目录布局→Task 1-2；§4 框架组件→Task 2-8；§5 四层套件→Task 8/15/16/17；§6 对比引擎→Task 3-4；§7 构建/更新/错误→Task 5/6/9/10/13；§9 验收→文末。✅ 全覆盖。

**2. 类型一致性：** `compare()` 签名（actual_blocks, baseline_blocks, tol）在 Task 4 定义，Task 8/13 调用一致；`CaseResult.status` 四态在 Task 7 定义，Task 8/12/report 使用一致；`Builder.build()` 返回 `dict[str,Path]` 在 Task 5 定义，Task 8/9/13 一致。✅

**3. 关键不变量（self-review 修正）：** exe 必须在**隔离工作目录** `<build_dir>/run/<case>` 跑，**绝不在 `case_dir` 直接跑**——否则产出 `.post.res` 覆盖同名基准，comparator 沦为"自己比自己"假 PASS。`E2ECase._prepare_work_dir`（Task 8）与 `update._run_in_work_dir`（Task 13）共同遵守此不变量；`test_e2e_work_dir_isolated_from_baseline`（Task 8）专项守护。同时修正了 `update.py` 在无旧基准时 `cr` 未定义的 NameError。

**4. 已知简化（诚实声明）：**
- Task 15 的 Catch2 断言为占位 `REQUIRE(true)`，需执行者读 `EquationSystem.h` 后补真实断言——因为 unit 测核心库 C++ 内部机制，签名须实测。
- Task 14 的 hel2d1/el2d1 的 `project`/`output` 名需实测 sample main（Step 1-2 已列检查步骤），不臆测。
- 跨构建容差档（1e-8）未在 config 暴露为开关——MVP 用同库 1e-12，跨构建需求出现时再加。
