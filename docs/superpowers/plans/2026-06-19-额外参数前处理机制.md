# 额外参数前处理读取机制 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把三层预留的 `_addParams` 接通为通用「额外参数前处理读取」机制，并让 DEl2D 的 Newmark `gamma/beta` 从 `.dat` 读取，替换 `NewmarkData.cpp:50` 的硬编码。

**Architecture:** 各层（FEMData/物理场/单元）用 `_addParams` 声明所需参数组（组名+参数名）；`gidPrePost::pre()` 开头收集声明表，解析 `.dat` 时按组名命中、读一行 double 回填到该层 `_paramValues`；各层用 `getParam(组名,参数名)` 取值。`.dat` 格式走项目级 GenData（参照 `time` 段）。pyTool 经 `MakerGidFile` 把 `preParams` 传给 `gidprb.j2`/`gidbas.j2` 生成对应段。

**Tech Stack:** C++14（CDFEG 核心库 DLL，Eigen）/ Python+Jinja2（pyTool）/ CMake（MinGW 或 MSVC）

---

## 注意事项（执行前必读）

1. **Git 规则**：本计划各 Task 末尾含 commit 步骤，提交注释一律**中文**。遵守用户全局规则——**提交前需用户确认**；若用 subagent-driven-development，在 task 间 review checkpoint 由用户确认提交。
2. **不可运行 `pyTool/test/testDEL2D.py`**：它会用 `ElQ4g` 名覆盖手写 `NewmarkQ4g` 的 C++ 源码（升级说明 5.11）。
3. **编码**：新增/修改的 C++ 源文件含中文注释，依赖根 CMake 的 `/utf-8`（MSVC）；新增源文件无需额外配置。
4. **构建**：FEMproject 用 CMake。示例命令假设 MinGW Makefiles；若用 MSVC 等价替换。测试 target `testPreParam` 仅供验证，全部通过后可在收尾 Task 选择保留或移除。
5. **设计依据**：见 `docs/superpowers/specs/2026-06-19-额外参数前处理机制-design.md`。

---

## 文件结构

**C++ 核心库（改）**
- `FEMproject/CDFEG/ElementBase.h` / `FemData.h` / `PhyFieldData.h`：各加 `_paramValues` 成员 + `getParam` 声明
- `FEMproject/CDFEG/ElementBase.cpp` / `FemData.cpp` / `PhyFieldData.cpp`：各实现 `getParam`
- `FEMproject/CDFEG/gidPrePost.h`：加 `_preParamDecls` 成员 + `collectPreParamDecls`/`readPreParams` 声明
- `FEMproject/CDFEG/gidPrePost.cpp`：实现收集/回填，`pre()` 接入

**C++ 测试（新建，可收尾移除）**
- `FEMproject/testPreParam/testPreParam.cpp` + `CMakeLists.txt`
- `FEMproject/CMakeLists.txt`：`add_subdirectory(testPreParam)`

**pyTool（改）**
- `pyTool/MakerGidFile.py`：`_initFromProject` 收集三层 `preParams` 到 `self.data`
- `pyTool/template/gidprb.j2`：项目级 GenData 的 `QUESTION` 段
- `pyTool/template/gidbas.j2`：`*GenData` 输出段

**DEl2D 示例（改）**
- `FEMproject/sample/DEl2D/NewmarkDispFieldData.cpp`：构造函数声明 `_addParams`
- `FEMproject/sample/DEl2D/NewmarkData.cpp`：`getParam` 取 `gamma/beta`
- `FEMproject/sample/DEl2D/del2d.dat`（新建）：验证用最小数据文件
- `FEMproject/sample/DEl2D/升级说明.md`：补充本次改动说明

---

## Task 1: C++ 三层 `_paramValues` 容器与 `getParam` 取值

**Files:**
- Modify: `FEMproject/CDFEG/ElementBase.h:111-112`、`FEMproject/CDFEG/FemData.h:144-145`、`FEMproject/CDFEG/PhyFieldData.h:150-151`
- Modify: `FEMproject/CDFEG/ElementBase.cpp`、`FEMproject/CDFEG/FemData.cpp`、`FEMproject/CDFEG/PhyFieldData.cpp`
- Create: `FEMproject/testPreParam/testPreParam.cpp`、`FEMproject/testPreParam/CMakeLists.txt`
- Modify: `FEMproject/CMakeLists.txt`

- [ ] **Step 1: 三个头文件各加成员与声明**

在 `ElementBase.h` 第 112 行 `_addParams` 声明**之后**（`};` 之前）追加：

```cpp
		// 从前处理读回的参数值，key=组名，value 按该组 _addParams 参数名顺序对齐
		std::map<std::string, std::vector<double>> _paramValues;
		// 取某组某参数的值；组或参数不存在、值未读到时返回 0.0
		double getParam(const std::string& group, const std::string& param) const;
```

在 `FemData.h` 第 145 行 `_addParams` 之后（`};` 之前）追加**完全相同**的两行。
在 `PhyFieldData.h` 第 151 行 `_addParams` 之后（`};` 之前）追加**完全相同**的两行。

> 三个头文件均已 `#include <map>` 与 `<vector>`、`<string>`，无需新增 include。

- [ ] **Step 2: 写失败测试程序**

创建 `FEMproject/testPreParam/testPreParam.cpp`：

```cpp
// 验证 _paramValues / getParam 取值逻辑（不依赖 dat 读取，直接手工填值）
#include "CDFEG/FemData.h"
#include "CDFEG/PhyFieldData.h"
#include "CDFEG/ElementBase.h"
#include <cassert>
#include <iostream>

int main() {
    using namespace CDFEG;
    FEMData* fd = new FEMData();
    PhyFieldData* f = new PhyFieldData(2, fd);
    ElementBase* e = new ElementBase(4, f);

    // 声明 + 手工填值（模拟 dat 已读回）
    f->_addParams = { {"newmark","gamma","beta"} };
    f->_paramValues["newmark"] = { 0.5, 0.25 };
    e->_addParams = { {"euler","theta"} };
    e->_paramValues["euler"] = { 1.5 };

    bool ok = true;
    ok &= (f->getParam("newmark","gamma") == 0.5);
    ok &= (f->getParam("newmark","beta")  == 0.25);
    ok &= (f->getParam("newmark","nope")  == 0.0);   // 组存在但无此参数名
    ok &= (f->getParam("other","x")       == 0.0);   // 组不存在
    ok &= (e->getParam("euler","theta")   == 1.5);

    std::cout << (ok ? "TEST PASS" : "TEST FAIL") << std::endl;
    return ok ? 0 : 1;
}
```

创建 `FEMproject/testPreParam/CMakeLists.txt`：

```cmake
add_executable(testPreParam testPreParam.cpp)
target_link_libraries(testPreParam PRIVATE CDFEG)
target_include_directories(testPreParam PRIVATE ${CMAKE_SOURCE_DIR})
```

- [ ] **Step 3: 注册测试 target 到根 CMake**

读 `FEMproject/CMakeLists.txt`，在其 `add_subdirectory(...)` 列表末尾（6 个示例之后）追加一行：

```cmake
add_subdirectory(testPreParam)
```

- [ ] **Step 4: 编译验证测试失败（TDD red）**

```bash
cd FEMproject
cmake -B build -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release
cmake --build build --target testPreParam
```
Expected: 编译失败——`getParam` 未定义（`undefined reference` 或声明无实现）。这确认测试在调用待实现接口。

- [ ] **Step 5: 三个 .cpp 实现 getParam**

在 `ElementBase.cpp` 的 `namespace CDFEG { ... }` 内（任意现有函数定义之后）加入：

```cpp
	double ElementBase::getParam(const std::string& group, const std::string& param) const {
		for (const auto& g : _addParams) {
			if (g.size() < 2 || g[0] != group) continue;
			for (size_t i = 1; i < g.size(); ++i) {
				if (g[i] == param) {
					auto it = _paramValues.find(group);
					if (it == _paramValues.end() || (i - 1) >= it->second.size()) return 0.0;
					return it->second[i - 1];
				}
			}
			return 0.0;
		}
		return 0.0;
	}
```

在 `FemData.cpp`、`PhyFieldData.cpp` 的 `namespace CDFEG { ... }` 内加入**相同逻辑**，仅把 `ElementBase::` 分别替换为 `FEMData::`、`PhyFieldData::`。

- [ ] **Step 6: 编译运行测试通过（TDD green）**

```bash
cmake --build build --target testPreParam
./build/testPreParam/testPreParam
```
Expected: 输出 `TEST PASS`，退出码 0。

- [ ] **Step 7: Commit**

```bash
git add FEMproject/CDFEG/ElementBase.h FEMproject/CDFEG/ElementBase.cpp \
        FEMproject/CDFEG/FemData.h FEMproject/CDFEG/FemData.cpp \
        FEMproject/CDFEG/PhyFieldData.h FEMproject/CDFEG/PhyFieldData.cpp \
        FEMproject/testPreParam FEMproject/CMakeLists.txt
git commit -m "核心库三层增加 _paramValues 容器与 getParam 取值"
```
（提交前请用户确认。）

---

## Task 2: gidPrePost 收集声明表 + readPreParams 回填

**Files:**
- Modify: `FEMproject/CDFEG/gidPrePost.h`
- Modify: `FEMproject/CDFEG/gidPrePost.cpp`
- Modify: `FEMproject/testPreParam/testPreParam.cpp`

- [ ] **Step 1: gidPrePost.h 加声明与成员**

在 `gidPrePost.h` 顶部 include 区（第 24 行 `#include "TextReader.h"` 之后）追加：

```cpp
#include <utility>
#include <set>
```

在 `readTime` 声明（第 45 行）之后追加两个方法声明：

```cpp
		int readPreParams(const std::string& group, const std::string& line);
		void collectPreParamDecls();
```

在 `_resItems` 成员（第 67 行）之后追加声明表成员：

```cpp
		// 额外参数声明表：组名(小写) → {目标层 _paramValues 指针, 该组 _addParams 行(首元素为原始组名)}
		std::map<std::string, std::pair<std::map<std::string,std::vector<double>>*, std::vector<std::string>>> _preParamDecls;
```

- [ ] **Step 2: 扩展测试程序（写读取回填的失败测试）**

把 `testPreParam.cpp` 的 `main` 替换为（保留 getParam 断言，新增 dat 读取断言）：

```cpp
#include "CDFEG/FemData.h"
#include "CDFEG/PhyFieldData.h"
#include "CDFEG/ElementBase.h"
#include "CDFEG/gidPrePost.h"
#include <iostream>

int main() {
    using namespace CDFEG;
    FEMData* fd = new FEMData();
    fd->_addParams = { {"proj","dim"} };
    PhyFieldData* f = new PhyFieldData(2, fd);
    f->_name = "F";
    f->_addParams = { {"newmark","gamma","beta"} };
    ElementBase* e = new ElementBase(4, f);
    e->_addParams = { {"euler","theta"} };
    f->_eleSubs.push_back(e);
    fd->_phyDatas.push_back(f);

    // 写临时 dat（只含额外参数段，验证读取回填；不依赖完整网格）
    std::ofstream dat("t.dat");
    dat << "** name=proj,structure=\"F64\"\n2.0 3.0\n";
    dat << "** name=newmark,structure=\"F64\"\n0.5 0.25\n";
    dat << "** name=euler,structure=\"F64\"\n1.5\n";
    dat.close();

    GidPrePost pp(fd);
    pp.setFilePath(".", "t");
    pp.pre();

    bool ok = true;
    ok &= (fd->getParam("proj","dim")          == 2.0);   // FEMData 层回填
    ok &= (f->getParam("newmark","gamma")      == 0.5);    // 物理场层回填
    ok &= (f->getParam("newmark","beta")       == 0.25);
    ok &= (e->getParam("euler","theta")        == 1.5);    // 单元层回填
    ok &= (f->getParam("newmark","nope")       == 0.0);

    std::cout << (ok ? "TEST PASS" : "TEST FAIL") << std::endl;
    return ok ? 0 : 1;
}
```
并在文件顶部 include 区补 `#include <fstream>`。

更新 `testPreParam/CMakeLists.txt`，给可执行文件设运行工作目录（使相对 `t.dat` 落在测试目录）：

```cmake
add_executable(testPreParam testPreParam.cpp)
target_link_libraries(testPreParam PRIVATE CDFEG)
target_include_directories(testPreParam PRIVATE ${CMAKE_SOURCE_DIR})
set_target_properties(testPreParam PROPERTIES
    RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR})
```

- [ ] **Step 3: 编译验证失败**

```bash
cmake --build build --target testPreParam
```
Expected: 编译失败——`collectPreParamDecls`/`readPreParams` 未定义。

- [ ] **Step 4: gidPrePost.cpp 实现收集与回填**

在 `gidPrePost.cpp` 顶部 include 区（第 24 行 `#include "ElementBase.h"` 之后）追加：

```cpp
#include <set>
```

在 `readTime` 实现（第 217 行 `}` 之后）插入两个函数：

```cpp
	void GidPrePost::collectPreParamDecls() {
		static const std::set<std::string> reserved = { "time","basedata","coord","id","ubf","elem" };
		auto addDecl = [&](auto* owner) {
			for (auto& g : owner->_addParams) {
				if (g.size() < 2) continue;
				const std::string& groupName = g[0];
				std::string gl = TextReader::toLowerCase(groupName);
				if (reserved.count(gl)) {
					std::cerr << "[GidPrePost] 跳过保留名参数组: " << groupName << std::endl;
					continue;
				}
				if (_preParamDecls.count(gl))
					std::cerr << "[GidPrePost] 参数组名重复(后者覆盖): " << groupName << std::endl;
				_preParamDecls[gl] = { &(owner->_paramValues), g };
			}
		};
		addDecl(_femData);
		for (PhyFieldData* f : _femData->_phyDatas) {
			addDecl(f);
			for (ElementBase* e : f->_eleSubs) addDecl(e);
		}
	}

	int GidPrePost::readPreParams(const std::string& group, const std::string& line) {
		auto it = _preParamDecls.find(group);
		if (it == _preParamDecls.end()) return -1;
		std::vector<double> vals = TextReader::splitDoubles(line, " ,");
		int nParams = (int)it->second.second.size() - 1;   // 参数名数 = 组行长度 - 1(组名)
		int n = (int)vals.size();
		if (n > nParams) n = nParams;
		std::vector<double> trimmed(vals.begin(), vals.begin() + n);
		const std::string& origName = it->second.second[0];   // 用原始组名回填，与 getParam 查询一致
		(*it->second.first)[origName] = trimmed;
		return 0;
	}
```

- [ ] **Step 5: pre() 接入收集与分发**

在 `gidPrePost.cpp` 的 `pre()` 内，`if (!_datReader.open()) return -1;`（第 131 行）之后、`const std::string& line = ...`（第 132 行）之前插入：

```cpp
		collectPreParamDecls();
```

在 `pre()` 的分发链中，`else if (typeLower == "elem") { readElement(params); }`（第 159-161 行）之后、闭合该 `if (line[0]=='*')` 块的 `}`（第 162 行）之前插入：

```cpp
				else if (_preParamDecls.count(nameLower)) {
					_datReader.readNextLine();
					readPreParams(nameLower, line);
				}
```

- [ ] **Step 6: 编译运行测试通过**

```bash
cmake --build build --target testPreParam
./build/testPreParam/testPreParam
```
Expected: `TEST PASS`，退出码 0。（`t.dat` 临时文件生成在测试二进制目录，可忽略。）

- [ ] **Step 7: Commit**

```bash
git add FEMproject/CDFEG/gidPrePost.h FEMproject/CDFEG/gidPrePost.cpp FEMproject/testPreParam/testPreParam.cpp FEMproject/testPreParam/CMakeLists.txt
git commit -m "gidPrePost 收集 _addParams 声明表并按组名回填 _paramValues"
```
（提交前请用户确认。）

---

## Task 3: pyTool 传递 preParams 与 gidprb/gidbas 模板

**Files:**
- Modify: `pyTool/MakerGidFile.py:44-75`（`_initFromProject`）
- Modify: `pyTool/template/gidprb.j2`
- Modify: `pyTool/template/gidbas.j2`

> `preParams` 结构约定：每项 `{"name":组名,"params":[参数名...],"defaults":[默认值...]}`；`defaults` 可选，缺则补 0.0。`toDict`/`fromDict` 已整体序列化 `preParams`（前序提交），含 `defaults` 键自动随之存取，无需再改。

- [ ] **Step 1: MakerGidFile 收集三层 preParams**

在 `pyTool/MakerGidFile.py` 的 `_initFromProject` 内，`self.batFn=path+"\\"+pro.name+".bat"`（第 75 行）之后、方法结束前插入：

```python
        # 收集 project/field/ele 三层 preParams，按组名去重，补全 defaults
        seen = set()
        preParams = []
        srcs = [pro]
        srcs += list(pro.fields)
        srcs += [e for f in pro.fields for e in f.eleSubs]
        for src in srcs:
            for pp in getattr(src, 'preParams', []):
                name = pp.get('name')
                if name is None or name in seen:
                    continue
                seen.add(name)
                params = list(pp.get('params', []))
                defaults = list(pp.get('defaults', []))
                if len(defaults) < len(params):
                    defaults += [0.0] * (len(params) - len(defaults))
                preParams.append({'name': name, 'params': params, 'defaults': defaults})
        self.data['preParams'] = preParams
```

同时给 `_initFromParams`（第 79-80 行）的 `self.data` 字典补一个键：把
```python
        self.data={"name":name,
        "dim":dim,"fields":[],"elems":[],"dbcs":[],"materials":[]}
```
改为
```python
        self.data={"name":name,
        "dim":dim,"fields":[],"elems":[],"dbcs":[],"materials":[],"preParams":[]}
```

并在 `_initFromProject` 第 51-52 行的初始 `self.data` 字典里追加 `"preParams":[]` 键（紧跟 `"bDynamic":False` 之后），保证模板在收集前也能访问该键。

- [ ] **Step 2: gidprb.j2 加 GenData 的 QUESTION 段**

在 `pyTool/template/gidprb.j2` 中，`{%endif%}`（time 段结束，第 7 行）之后、`{%for mat in materials%}`（第 8 行）之前插入：

```
{% for pp in preParams %}
{% for p in pp.params %}
QUESTION: {{ p }}
VALUE: {{ pp.defaults[loop.index0] }}
{% endfor %}
{% endfor %}
```

- [ ] **Step 3: gidbas.j2 加 *GenData 输出段**

在 `pyTool/template/gidbas.j2` 中，`{% endif %}`（time 段结束，第 6 行）之后、`{%for mat in materials%}`（第 7 行）之前插入：

```
{% for pp in preParams %}** name={{ pp.name }},structure="F64"
{% for p in pp.params %}*GenData({{ p }}) {% endfor %}

{% endfor %}
```

- [ ] **Step 4: 回归测试——既有 testEl2D.py 不应生成多余段**

```bash
cd pyTool
python test/testEl2D.py
```
打开生成的 `FEMproject/sample/El2D/el.gid/el.bas` 与 `el.prb`，确认：**不含** `*GenData(gamma)`/`QUESTION: gamma` 等额外参数段（El2D 的 `preParams` 为空，模板的 `{% for pp in preParams %}` 应渲染为空）。同时确认既有 `time`/`materials`/`coord` 段无破坏。

- [ ] **Step 5: 正向测试——带 preParams 的临时 project**

在 `pyTool/` 下临时运行：

```bash
cd pyTool
python -c "
from DataProject import DataProject
from DataField import DataField
from MakerGidFile import MakerGidFile
import os
pro = DataProject('tmp', 2)
f = DataField('D')
f.preParams = [{'name':'newmark','params':['gamma','beta'],'defaults':[0.5,0.25]}]
pro.addField(f)
m = MakerGidFile(pro, 'tmp_out')
m.makeBas(); m.makePrb()
print('--- bas ---'); print(open('tmp_out/tmp.gid/tmp.bas',encoding='utf-8').read())
print('--- prb ---'); print(open('tmp_out/tmp.gid/tmp.prb',encoding='utf-8').read())
"
```
Expected: `.bas` 含 `** name=newmark,structure="F64"` 行及 `*GenData(gamma) *GenData(beta)`；`.prb` 含 `QUESTION: gamma / VALUE: 0.5` 与 `QUESTION: beta / VALUE: 0.25`。

验证后删除临时产物：`rm -rf pyTool/tmp_out`。

- [ ] **Step 6: Commit**

```bash
git add pyTool/MakerGidFile.py pyTool/template/gidprb.j2 pyTool/template/gidbas.j2
git commit -m "pyTool 传递 preParams 并经 gidprb/gidbas 生成额外参数 GenData 段"
```
（提交前请用户确认。注意：不要 `git add` testEl2D.py 重新覆盖的 El2D 手写源码——若 testEl2D.py 运行改动了 sample/El2D 的 C++ 文件，需先与用户确认是否纳入。）

---

## Task 4: DEl2D 接入（声明 _addParams + getParam 取值）

**Files:**
- Modify: `FEMproject/sample/DEl2D/NewmarkDispFieldData.cpp:24-34`
- Modify: `FEMproject/sample/DEl2D/NewmarkData.cpp:44-50`

- [ ] **Step 1: NewmarkDispFieldData 声明 _addParams**

在 `NewmarkDispFieldData.cpp` 构造函数末尾（`_resForm = "Vector OnNodes";` 第 33 行之后、`}` 之前）追加：

```cpp
    // 声明从前处理读取的 Newmark 积分参数组（gamma/beta）
    _addParams = { {"newmark","gamma","beta"} };
```

- [ ] **Step 2: NewmarkData.cpp 改用 getParam 取值**

把 `NewmarkData.cpp` 第 44-50 行：

```cpp
    // 传递 Newmark 参数：dt/tmax 来自 dat 的 time 段（FEMData 持有），gamma/beta 默认 0.5/0.25
    if (_dt <= 0.0)
    {
        std::cerr << "[NewmarkData] dt <= 0, 请检查 dat 文件 time 段" << std::endl;
        return -1;
    }
    aField->setNewmarkParams(0.5, 0.25, _dt);
```

替换为：

```cpp
    // dt/tmax 来自 dat 的 time 段（FEMData 持有）；gamma/beta 来自 dat 的 newmark 段（物理场 _addParams 声明）
    if (_dt <= 0.0)
    {
        std::cerr << "[NewmarkData] dt <= 0, 请检查 dat 文件 time 段" << std::endl;
        return -1;
    }
    double gamma = aField->getParam("newmark","gamma");
    double beta  = aField->getParam("newmark","beta");
    if (beta <= 0.0)
    {
        std::cerr << "[NewmarkData] beta <= 0, 请检查 dat 文件 newmark 段" << std::endl;
        return -1;
    }
    aField->setNewmarkParams(gamma, beta, _dt);
```

- [ ] **Step 3: 编译 DEl2D**

```bash
cd FEMproject
cmake --build build --target del2d
```
Expected: 编译成功（`NewmarkDispFieldData` 继承 `getParam`，`NewmarkData` 调用合法）。

- [ ] **Step 4: Commit**

```bash
git add FEMproject/sample/DEl2D/NewmarkDispFieldData.cpp FEMproject/sample/DEl2D/NewmarkData.cpp
git commit -m "DEl2D 声明 newmark 参数组并从 dat 读取 gamma/beta 替换硬编码"
```
（提交前请用户确认。）

---

## Task 5: 端到端验证（手写 dat + 改参数验证）+ 文档收尾

**Files:**
- Create: `FEMproject/sample/DEl2D/del2d.dat`
- Modify: `FEMproject/sample/DEl2D/升级说明.md`

- [ ] **Step 1: 新建最小验证 dat**

创建 `FEMproject/sample/DEl2D/del2d.dat`（4 节点 1 个 Q4 单元悬臂板，固定左边 2 节点；材料对齐 `NewmarkQ4g._paramNames = {pe,pv,fu,fv,rou,alpha}`）：

```
** name=baseData,structure="I32"
4 1
** name=time,structure="F64"
0.1 1.0
** name=newmark,structure="F64"
0.5 0.25
** name=mat_NewmarkQ4g,structure="F64**6",type="mat",index=1
1000.0 0.3 0.0 0.0 1.0 0.05
** name=coord,structure="I32**1 F64**2",type="coord",index=1
1 0.0 0.0
2 1.0 0.0
3 1.0 1.0
4 0.0 1.0
** name=ubfNewmarkDisp,structure="I32**1 F64**2",type="ubf",index=1
1 0.0 0.0
4 0.0 0.0
** name=NewmarkQ4g,structure="I32**6",type="elem",index=1
1 1 2 3 4 1
```

- [ ] **Step 2: 运行 DEl2D 读取 dat**

```bash
cd FEMproject/sample/DEl2D
./del2d del2d .
```
（即 `argv[1]=del2d`、`argv[2]=.`，读取 `./del2d.dat`。）
Expected: 程序正常运行，无 `[NewmarkData] beta <= 0` 或 `dt <= 0` 报错；生成 `del2d.post.msh`/`del2d.post.res`。

- [ ] **Step 3: 验证参数确实从文件读取（改值看结果变化）**

把 `del2d.dat` 中 newmark 段改为 `0.5 0.0`（beta=0），重新运行：
```bash
./del2d del2d .
```
Expected: 输出 `[NewmarkData] beta <= 0, 请检查 dat 文件 newmark 段` 并提前返回（证明 beta 取自文件而非硬编码 0.25）。

恢复 newmark 段为 `0.5 0.25`；再改为 `0.6 0.4` 重新运行，确认程序正常完成（不同 beta 产生不同 `.post.res`）。验证后恢复为 `0.5 0.25`。

- [ ] **Step 4: 更新升级说明.md**

在 `FEMproject/sample/DEl2D/升级说明.md` 第五节末尾（5.12 之后）追加一节：

```markdown
### 5.13 Newmark 参数 gamma/beta 改为前处理读取

`gamma/beta` 不再硬编码于 `NewmarkData::caculate`，改由 `NewmarkDispFieldData::_addParams = {{"newmark","gamma","beta"}}` 声明，`gidPrePost::pre()` 读 dat 的 `** name=newmark` 段回填到 `_paramValues`，`caculate` 用 `getParam("newmark",...)` 取值。dat 须含：

    ** name=newmark,structure="F64"
    0.5 0.25

缺该段时 `getParam` 返回 0.0，`beta<=0` 会被 `caculate` 拦截报错。该机制由核心库 `_addParams`/`_paramValues`/`getParam` 通用支持，任意层（FEMData/物理场/单元）均可声明参数组。
```

- [ ] **Step 5: 清理可选测试 target（可选）**

若不保留 `testPreParam` 作为回归测试：从 `FEMproject/CMakeLists.txt` 移除 `add_subdirectory(testPreParam)`，删除 `FEMproject/testPreParam/` 目录，并 `git rm` 之。若保留则跳过本步。

- [ ] **Step 6: Commit**

```bash
git add FEMproject/sample/DEl2D/del2d.dat FEMproject/sample/DEl2D/升级说明.md
git commit -m "DEl2D 增加 newmark 参数验证 dat 并补充升级说明"
```
（提交前请用户确认。`del2d.post.msh`/`del2d.post.res` 等运行产物不应提交，确认 `.gitignore` 已忽略或在 add 时排除。）

---

## 验收标准

- [ ] `testPreParam` 三层 `_paramValues` 回填 + `getParam` 取值全部 PASS。
- [ ] DEl2D 用 `del2d.dat` 跑通；改 dat 中 `gamma/beta` 使结果变化（beta=0 被拦截），证明取自文件。
- [ ] `testEl2D.py` 回归：El2D 的 `.bas/.prb` 不含多余参数段。
- [ ] 带 `preParams` 的临时 project 能生成正确的 `*GenData`/`QUESTION` 段。
- [ ] 全程未运行 `testDEL2D.py`。

---

## Self-Review 记录

（plan 作者自审，已在编写时核对）

- **Spec 覆盖**：设计文档第一节端到端数据流的 5 层——pyTool 定义层（Task 3）、模板层（Task 3）、dat（Task 5）、gidPrePost 读取回填（Task 2）、各层取值（Task 1+4）均有对应 Task。
- **类型一致性**：`_paramValues` 类型 `map<string,vector<double>>`、`getParam(string,string)const→double`、`_preParamDecls` 的 pair 第二元素为含原始组名的 `_addParams` 行（`readPreParams` 用 `[0]` 回填，`getParam` 用 `g[0]` 查询）——三处一致。
- **占位符扫描**：无 TBD/TODO；每步含完整代码或确切命令与期望输出。
- **风险点**：Task 3 Step 6 已提示 testEl2D.py 可能改动 El2D 源码的风险；Task 5 Step 6 提示运行产物不入库。
