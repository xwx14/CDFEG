# 材料本构类型彻底迁移（P1）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 彻底删除 `ElementBase._paramNames` 与 readMate 回退，改由 `DataProject.makeData` 自动建本构表（单物理场用单元名、多物理场用 gidName 保序合并去重），全部 sample 迁移到本构表驱动。

**Architecture:** 分两段——先在核心库**保留** `_paramNames` 期间迁移所有 sample（每 sample 改完即可回归，readMate 走本构表）；最后删核心库 `_paramNames` + readMate 回退。pyTool `makeData` 自动建表取代 P0 的 `_applyMateTypeCompat`。

**Tech Stack:** C++14（核心库 DLL，MinGW）、Python 3 + Jinja2（pyTool）、pytest、CDFEG 测试框架（`test/run_tests.py`）。

## Global Constraints

- C++14；头文件 `#ifndef/#define/#endif`；类成员 `_` 前缀；MSVC `/utf-8`。
- 命名空间 `CDFEG::`，导出宏 `CDFEG_API`。
- Python 驼峰命名；Jinja2 模板。
- 编译工具链：MinGW `C:/dev/mingw64`（`mingw32-make.exe`，`make` 不在 PATH），cmake `D:/greensoft/cmake-4.1.1`；构建须 `-DCMAKE_MAKE_PROGRAM=C:/dev/mingw64/bin/mingw32-make.exe`。
- 材料段头 `name` = 本构类型名（不带 `mat_` 前缀，P0 已统一）。
- 提交注释中文 conventional commit；当前分支 main（P0 已合并），本计划在新分支 `feat/mate-p1` 执行。

## File Structure

**核心库（`FEMproject/CDFEG/`）：**
- `ElementBase.h` — 删 `_paramNames`（Task 8）
- `gidPrePost.cpp` — readMate 删回退（Task 8）

**pyTool（`pyTool/`）：**
- `DataProject.py` — `makeData` 建本构表 + `_mergeOrAddMateType`（Task 1）
- `MakerCpp.py` — 移除 `_applyMateTypeCompat` + `makeProject` 调 `project.makeData()`（Task 1）
- `tests/test_mate_types.py` — makeData 测试（Task 1）

**sample（`FEMproject/sample/`）：** Task 3-7 改单元构造 + DomainData 构造。

---

## Task 1: pyTool makeData 自动建本构表

**Files:**
- Modify: `pyTool/DataProject.py`（`makeData` + 新增 `_mergeOrAddMateType`）
- Modify: `pyTool/MakerCpp.py`（移除 `_applyMateTypeCompat` + 其调用；`makeProject` 加 `project.makeData()`）
- Modify: `pyTool/tests/test_mate_types.py`（删 3 个 `_applyMateTypeCompat` 测试，加 3 个 makeData 测试）

**Interfaces:**
- Produces: `DataProject.makeData()` 建本构表 + 设 `ele.mateTypeName`；`DataProject._mergeOrAddMateType(name, params, values)` 保序合并去重。Task 2-7 依赖（test*.py 用 paramNames+gidName，makeData 自动建）。

- [ ] **Step 1: 改 `test_mate_types.py`——删旧测试、加 makeData 失败测试**

删除文件中 3 个 `_applyMateTypeCompat` 测试（`test_applyMateTypeCompat_*`）与 `from MakerCpp import MakerCpp` import。追加：
```python
from DataProject import DataProject
from DataField import DataField
from DataEleSub import DataEleSub


def test_makeData_singleField_uses_ele_name():
    p = DataProject("p", 1)
    f = p.addField("F")
    e = DataEleSub("Truss1D", 2); e.paramNames = ["E", "A"]; e.paramValues = [1.0, 2.0]
    f.addEleSub(e)
    p.makeData()
    assert e.mateTypeName == "Truss1D"
    assert p.mateTypes == [{"name": "Truss1D", "params": ["E", "A"], "defaults": [1.0, 2.0]}]


def test_makeData_multiField_uses_gidName_and_merges_dedup():
    p = DataProject("p", 2)
    f1 = p.addField("Heat"); f2 = p.addField("Del")
    e1 = DataEleSub("HeatQ4g", 4); e1.gidName = "HelQ4g"; e1.paramNames = ["ek", "ec", "q"]
    e2 = DataEleSub("DelQ4g", 4); e2.gidName = "HelQ4g"; e2.paramNames = ["pe", "pv", "ek"]
    f1.addEleSub(e1); f2.addEleSub(e2)
    p.makeData()
    assert e1.mateTypeName == "HelQ4g" and e2.mateTypeName == "HelQ4g"
    assert len(p.mateTypes) == 1
    assert p.mateTypes[0]["name"] == "HelQ4g"
    assert p.mateTypes[0]["params"] == ["ek", "ec", "q", "pe", "pv"]  # ek 保序去重


def test_makeData_singleField_multi_ele_separate_constitutives():
    p = DataProject("p", 2)
    f = p.addField("F")
    e1 = DataEleSub("ElQ4g", 4); e1.paramNames = ["pe"]
    e2 = DataEleSub("StressBL2g", 2); e2.paramNames = ["fu", "fv"]
    f.addEleSub(e1); f.addEleSub(e2)
    p.makeData()
    assert {mt["name"] for mt in p.mateTypes} == {"ElQ4g", "StressBL2g"}
    assert e1.mateTypeName == "ElQ4g" and e2.mateTypeName == "StressBL2g"
```
（保留文件顶部原有的 `import os`/`import jinja2`/`TEMPLATE_DIR`/`_render` 等。）

- [ ] **Step 2: 运行确认失败**

Run: `cd pyTool && python -m pytest tests/test_mate_types.py -v`
Expected: 3 个新 makeData 用例 FAIL（makeData 未建本构表）；原 DataProject/DataEleSub/模板用例仍 PASS。

- [ ] **Step 3: `DataProject.py` 改 `makeData` + 加 `_mergeOrAddMateType`**

替换 `makeData`：
```python
    def makeData(self):
        for field in self.fields:
            field.makeData()
        # 建立本构表：单物理场用单元 name，多物理场用 gidName（同 key 保序合并去重）
        self.mateTypes = []
        multiField = len(self.fields) > 1
        for field in self.fields:
            for ele in field.eleSubs:
                constitutiveName = ele.gidName if multiField else ele.name
                ele.mateTypeName = constitutiveName
                self._mergeOrAddMateType(constitutiveName, ele.paramNames, ele.paramValues)

    def _mergeOrAddMateType(self, name, params, values):
        """注册或合并本构类型：同名保序合并去重，否则新增。"""
        for mt in self.mateTypes:
            if mt['name'] == name:
                seen = set(mt['params'])
                for i, p in enumerate(params):
                    if p not in seen:
                        seen.add(p)
                        mt['params'].append(p)
                        mt['defaults'].append(values[i] if i < len(values) else 0.0)
                return
        self.addMateType(name, params, values)
```

- [ ] **Step 4: `MakerCpp.py` 移除 `_applyMateTypeCompat` + `makeProject` 调 `makeData`**

删除 `_applyMateTypeCompat` 静态方法整体；删除 `_build_render_data` 末尾的 `self._applyMateTypeCompat(project)` 行。

在 `makeProject`（约 `MakerCpp.py:206`）的 `self._build_render_data(project)` 之后、`file_lists = {"h": [], "cpp": []}` 之前，插入：
```python
        project.makeData()
```
（确保 `_makeDomainData` 用 `project.toDict()` 前，`mateTypes`/`mateTypeName` 已就绪。）

- [ ] **Step 5: 运行测试全绿**

Run: `cd pyTool && python -m pytest tests/test_mate_types.py -v`
Expected: 全 PASS（makeData 3 用例 + 原 DataProject/DataEleSub/模板用例）。

- [ ] **Step 6: 提交**

```bash
git add pyTool/DataProject.py pyTool/MakerCpp.py pyTool/tests/test_mate_types.py
git commit -m "feat(pyTool): makeData 自动建本构表（单场用单元名/多场用 gidName 合并去重），取代 _applyMateTypeCompat"
```

---

## Task 2: 测 truss1D 框架生成

**Files:**
- 验证产物：`pyTool/test/truss1D/Truss1D/Truss1D.cpp`、`pyTool/test/truss1D/Truss1D/Truss1DData.cpp`（.gitignore 忽略，不入库）

**Interfaces:**
- Consumes: Task 1 makeData + 模板（P0 elesub/domaindata）

- [ ] **Step 1: 运行 test1DTruss.py 生成框架**

Run: `cd pyTool && python test/test1DTruss.py`
Expected: 输出到 `pyTool/test/truss1D/`（new 模式，含 CDFEG/third 副本 + Truss1D/ 项目）。无报错。

- [ ] **Step 2: 检查产物含新机制、无旧机制**

Run:
```bash
grep -c "_mateTypeName" pyTool/test/truss1D/Truss1D/Truss1D.cpp
grep -c "_mateConstitutive" pyTool/test/truss1D/Truss1D/Truss1DData.cpp
grep -c "_paramNames" pyTool/test/truss1D/Truss1D/Truss1DData.cpp pyTool/test/truss1D/Truss1D/Truss1D.cpp
```
Expected: Truss1D.cpp 含 `_mateTypeName = "Truss1D";`（count≥1）；Truss1DData.cpp 含 `_mateConstitutive["Truss1D"] = { "E", "A" };`（count≥1）；两文件 `_paramNames` count=0。

- [ ] **Step 3: 编译生成的 truss1D 独立项目验证**

Run:
```bash
cd pyTool/test/truss1D
"D:/greensoft/cmake-4.1.1/bin/cmake.exe" -B build -G "MinGW Makefiles" -DCMAKE_MAKE_PROGRAM=C:/dev/mingw64/bin/mingw32-make.exe
"D:/greensoft/cmake-4.1.1/bin/cmake.exe" --build build -j
```
Expected: 编译成功（验证 pyTool 生成的框架用新机制可编译）。

- [ ] **Step 4: 提交（仅 testHel2d.py 等已跟踪文件的改动；truss1D 产物 .gitignore 不入库）**

本任务无已跟踪文件改动（仅验证），跳过 commit。在进度账本记录"Task 2: 验证通过"。

---

## Task 3: truss1D/2D/3D sample 改造（makeData 入口）

**Files:**
- Modify: `FEMproject/sample/truss1D/Truss1D.cpp`、`FEMproject/sample/truss1D/Truss1DData.cpp`
- Modify: `FEMproject/sample/truss2D/Truss2D.cpp`、`FEMproject/sample/truss2D/Truss2DData.cpp`
- Modify: `FEMproject/sample/truss3D/Truss3D.cpp`、`FEMproject/sample/truss3D/Truss3DData.cpp`

**Interfaces:**
- Consumes: 核心库 `_mateTypeName`（P0）、`_mateConstitutive`（P0）；`_paramNames` 仍保留（Task 8 删）

- [ ] **Step 1: 各单元构造删 `_paramNames`、加 `_mateTypeName`**

`truss1D/Truss1D.cpp`（约 24 行）：
```cpp
// 删：_paramNames ={ "E","A"};
// 加：
_mateTypeName = "Truss1D";
```
`truss2D/Truss2D.cpp`、`truss3D/Truss3D.cpp` 同理（本构名 `Truss2D`/`Truss3D`，参数 `["E","A"]` 已在原 `_paramNames`，删行加 `_mateTypeName`）。

- [ ] **Step 2: 各 DomainData 构造加本构注册**

`Truss1DData.cpp`（`Truss1DData::Truss1DData()`，约 20-23 行 `_phyDatas.push_back` 后）：
```cpp
    _mateConstitutive["Truss1D"] = { "E", "A" };
```
`Truss2DData.cpp` 加 `_mateConstitutive["Truss2D"] = { "E", "A" };`；`Truss3DData.cpp` 加 `_mateConstitutive["Truss3D"] = { "E", "A" };`。

- [ ] **Step 3: 编译 + 回归 truss1D/2D/3D**

Run: `python test/run_tests.py --suite e2e --case truss1D1` （同样跑 truss2D1、truss3D1，或一次 `--suite e2e`）
Expected: truss1D/2D/3D PASS（makeData 入口不经 readMate，但单元编译通过；本构注册无害）。

- [ ] **Step 4: 提交**

```bash
git add FEMproject/sample/truss1D/ FEMproject/sample/truss2D/ FEMproject/sample/truss3D/
git commit -m "feat(truss): 单元改用 _mateTypeName，DomainData 注册 _mateConstitutive（删 _paramNames 赋值）"
```

---

## Task 4: El2D sample 改造（GiD，3 单元）

**Files:**
- Modify: `FEMproject/sample/El2D/ElQ4g.cpp`、`ElT3g.cpp`、`StressBL2g.cpp`、`elData.cpp`

- [ ] **Step 1: 三单元构造删 `_paramNames`、加 `_mateTypeName`**

- `ElQ4g.cpp`（约 26 行）：删 `_paramNames = { "pe", "pv", "fu", "fv", "rou", "alpha" };`，加 `_mateTypeName = "ElQ4g";`
- `ElT3g.cpp`（约 25 行）：删 `_paramNames ={...};`，加 `_mateTypeName = "ElT3g";`
- `StressBL2g.cpp`（约 26 行）：删 `_paramNames = { "fu", "fv" };`，加 `_mateTypeName = "StressBL2g";`

- [ ] **Step 2: `elData.cpp` 构造加 3 本构注册**

在 `elData::elData()` 的 `_phyDatas.push_back(...)` 后加：
```cpp
    _mateConstitutive["ElQ4g"] = { "pe", "pv", "fu", "fv", "rou", "alpha" };
    _mateConstitutive["ElT3g"] = { "pe", "pv", "fu", "fv", "rou", "alpha" };
    _mateConstitutive["StressBL2g"] = { "fu", "fv" };
```

- [ ] **Step 3: 编译 + 回归**

Run: `python test/run_tests.py --suite e2e` （el2d1/el2d_bf1/el2_mfel1 都跑）
Expected: el2d1/el2d_bf1/el2_mfel1 PASS（readMate 本构表命中 3 本构）。

> 若 el2d1 仍因既有 Eigen bug skip（见 P0 账本），不阻塞；el2d_bf1/el2_mfel1 需 PASS。

- [ ] **Step 4: 提交**

```bash
git add FEMproject/sample/El2D/
git commit -m "feat(El2D): 三单元改用 _mateTypeName，elData 注册 3 本构（删 _paramNames 赋值）"
```

---

## Task 5: ElT3 sample 改造（GiD，1 单元）

**Files:**
- Modify: `FEMproject/sample/ElT3/ElT3.cpp`、`ElasticT3Data.cpp`

- [ ] **Step 1: `ElT3.cpp` 删 `_paramNames`、加 `_mateTypeName`**

`ElT3.cpp`（约 21 行）：删 `_paramNames = { "E", "nu","t", "fx", "fy"};`，加 `_mateTypeName = "ElT3";`

- [ ] **Step 2: `ElasticT3Data.cpp` 构造加本构注册**

`ElasticT3Data::ElasticT3Data()`（约 4-7 行 `_phyDatas.push_back` 后）加：
```cpp
    _mateConstitutive["ElT3"] = { "E", "nu", "t", "fx", "fy" };
```

- [ ] **Step 3: 编译 + 回归**

Run: `python test/run_tests.py --suite e2e --case elt3_1` （也跑 elt3_4x4）
Expected: elt3_1/elt3_4x4 PASS。

- [ ] **Step 4: 提交**

```bash
git add FEMproject/sample/ElT3/
git commit -m "feat(ElT3): 单元改用 _mateTypeName，ElasticT3Data 注册本构（删 _paramNames 赋值）"
```

---

## Task 6: DEl2D sample 改造（GiD，2 单元）

**Files:**
- Modify: `FEMproject/sample/DEl2D/DelQ4g.cpp`、`StressBL2g.cpp`、`del2dData.cpp`

- [ ] **Step 1: 两单元构造删 `_paramNames`、加 `_mateTypeName`**

- `DelQ4g.cpp`（约 27 行）：删 `_paramNames = { "pe", "pv", "fu", "fv", "rou", "alpha" };`，加 `_mateTypeName = "DelQ4g";`
- `StressBL2g.cpp`（约 26 行）：删 `_paramNames = { "fu", "fv" };`，加 `_mateTypeName = "StressBL2g";`

- [ ] **Step 2: `del2dData.cpp` 构造加 2 本构注册**

`del2dData::del2dData()`（约 22-27 行 `_phyDatas.push_back` 后）加：
```cpp
    _mateConstitutive["DelQ4g"] = { "pe", "pv", "fu", "fv", "rou", "alpha" };
    _mateConstitutive["StressBL2g"] = { "fu", "fv" };
```

- [ ] **Step 3: 编译 + 回归**

Run: `python test/run_tests.py --suite e2e --case del2d1`
Expected: del2d1 PASS（max|Δ|=0）。

- [ ] **Step 4: 提交**

```bash
git add FEMproject/sample/DEl2D/
git commit -m "feat(DEl2D): 两单元改用 _mateTypeName，del2dData 注册 2 本构（删 _paramNames 赋值）"
```

---

## Task 7: Hel2D testHel2d.py 恢复 paramNames+gidName

**Files:**
- Modify: `pyTool/test/testHel2d.py`

- [ ] **Step 1: 删手动 addMateType、恢复 paramNames+gidName 写法**

删除 `project.addMateType("HelQ4g", [...10参数...], defaults=[...])` 整块。

`hele1`（HeatQ4g）改回：
```python
hele1.gidName = "HelQ4g"
hele1.dispNames = ["T"]
hele1.paramNames = ["ek", "ec", "q"]
hele1.paramValues = [1.0, 1.0, 0.0]
```
（删 `hele1.mateTypeName = "HelQ4g"`——makeData 自动设。）

`dele1`（DelQ4g）改回：
```python
dele1.gidName = "HelQ4g"
dele1.dispNames = ["u", "v"]
dele1.eleResNames = ["sigmaXX", "sigmaYY", "sigmaXY", "volume"]
dele1.paramNames = ["pe", "pv", "fu", "fv", "rou", "alpha", "alfa"]
dele1.paramValues = [1.0e10, 0.3, 0.0, 0.0, 3000.0, 0.6, 0.6]
```
（删 `dele1.mateTypeName = "HelQ4g"`。）

- [ ] **Step 2: 运行 testHel2d.py 生成（验证 makeData 自动建 HelQ4g 本构）**

Run: `cd pyTool && python test/testHel2d.py`
Expected: 生成 `FEMproject/sample/Hel2D/`。检查：
```bash
grep "_mateConstitutive\[\"HelQ4g\"\]" FEMproject/sample/Hel2D/hel2dData.cpp
grep "_mateTypeName = \"HelQ4g\"" FEMproject/sample/Hel2D/HeatQ4g.cpp FEMproject/sample/Hel2D/DelQ4g.cpp
```
Expected: hel2dData 含 `_mateConstitutive["HelQ4g"] = { "ek", "ec", "q", "pe", "pv", "fu", "fv", "rou", "alpha", "alfa" };`（makeData 保序合并）；两单元 `_mateTypeName = "HelQ4g";`。

> **注意**：testHel2d.py makeAll 会重生成 .cpp 覆盖手填 run/uEle。本任务运行前，确认 HeatQ4g.cpp/DelQ4g.cpp 的 run/uEle 已在 testHel2d.py 之外手填保留——若 testHel2d.py 无 runCode/uCode，重生成会丢失。**本步骤仅验证 makeData 建表，运行后用 git 恢复 .cpp 手填版本**：
```bash
git checkout -- FEMproject/sample/Hel2D/HeatQ4g.cpp FEMproject/sample/Hel2D/DelQ4g.cpp FEMproject/sample/Hel2D/hel2dData.cpp FEMproject/sample/Hel2D/HeatFieldData.cpp FEMproject/sample/Hel2D/DelDispFieldData.cpp
```
仅保留 testHel2d.py 的改动（+ 可能的 hel2dData 本构注册手填补充）。

- [ ] **Step 3: 手工确认 hel2dData.cpp 含本构注册（P0 已加，保持）**

Run: `grep "_mateConstitutive" FEMproject/sample/Hel2D/hel2dData.cpp`
Expected: 含 `_mateConstitutive["HelQ4g"] = { "ek", "ec", "q", "pe", "pv", "fu", "fv", "rou", "alpha", "alfa" };`（P0 已加，git 恢复后仍在）。

- [ ] **Step 4: 回归 hel2d1**

Run: `python test/run_tests.py --suite e2e --case hel2d1`
Expected: hel2d1 PASS（max|Δ|=0）。

- [ ] **Step 5: 提交**

```bash
git add pyTool/test/testHel2d.py
git commit -m "feat(Hel2D): testHel2d 恢复 paramNames+gidName，由 makeData 自动建 HelQ4g 本构"
```

---

## Task 8: 核心库删 _paramNames + readMate 回退

**Files:**
- Modify: `FEMproject/CDFEG/ElementBase.h`、`FEMproject/CDFEG/gidPrePost.cpp`

> 前置：Task 3-7 所有 sample 已不引用 `_paramNames`（单元删赋值，readMate 走本构表）。本任务删核心库旧机制。

- [ ] **Step 1: `ElementBase.h` 删 `_paramNames` 成员**

删除（约 109 行）：
```cpp
		// 单元子程序所需的材料参数名称
		std::vector<std::string> _paramNames;
```
保留 `_mateTypeName`（P0 新增，紧邻其后）。

- [ ] **Step 2: `gidPrePost.cpp` readMate 删回退分支**

替换 `readMate` 函数体为（只走本构表）：
```cpp
int GidPrePost::readMate(const std::map<std::string, std::string>& params)
{
	std::string name = params.at("name");
	auto itType = _femData->_mateConstitutive.find(name);
	if (itType == _femData->_mateConstitutive.end()) return -1;
	const std::vector<std::string>& mateparams = itType->second;
	const std::string& line = _datReader.getCurrentLine();
	int i = 0;
	while (_datReader.readNextLine()) {
		if (line[0] == '*') {
			_datReader.preLine();
			break;
		}
		std::vector<double> vals = TextReader::splitDoubles(line, " ,");
		int len1 = vals.size();
		if (len1 > (int)mateparams.size())len1 = mateparams.size();
		std::map<std::string, double> paramMap;
		for (int j = 0; j < len1; ++j) {
			paramMap[mateparams[j]] = vals[j];
		}
		_femData->addMate(paramMap, name+"_" + std::to_string(++i));
	}
	return 0;
}
```

- [ ] **Step 3: 编译核心库 + 全 sample**

Run: `python test/run_tests.py --suite e2e`
Expected: 编译成功（无 `_paramNames` 引用）；所有 e2e PASS。

> 若编译报 `_paramNames` 未定义，说明有 sample 单元残留 `_paramNames` 赋值——回到 Task 3-7 补删。

- [ ] **Step 4: 提交**

```bash
git add FEMproject/CDFEG/ElementBase.h FEMproject/CDFEG/gidPrePost.cpp
git commit -m "feat(core): 删除 ElementBase._paramNames 与 readMate 回退，材料只走本构表"
```

---

## Task 9: 全量回归 + final 验证

- [ ] **Step 1: 全量回归**

Run: `python test/run_tests.py`
Expected: 11 pass / 0 fail / 0 error（10 e2e + unit.core）；位移/应力 max|Δ|=0。

- [ ] **Step 2: 确认无残留 `_paramNames`**

Run: `grep -rn "_paramNames" FEMproject/ --include=*.cpp --include=*.h | grep -v "test/truss1D/CDFEG"`
Expected: 仅 `test/truss1D/CDFEG/` 旧快照（new 模式重生成会覆盖，不入库）；主源码无残留。

- [ ] **Step 3: 更新进度账本，准备 final review**

无 commit（验证任务）。

---

## Self-Review

**1. Spec 覆盖**：spec §4（核心库）→ Task 8 ✓；§5（pyTool makeData）→ Task 1 ✓；§6（sample）→ Task 3-7 ✓；§7（测试）→ Task 2/9 ✓。

**2. 占位符**：无 TBD；每步含完整代码/命令。

**3. 类型一致**：`makeData`/`_mergeOrAddMateType`（Task 1）→ Task 2-7 依赖；`_mateTypeName`/`_mateConstitutive`（P0）全程一致；Task 8 删 `_paramNames` 前 Task 3-7 已清所有引用。

**4. 顺序稳健**：Task 3-7 sample 改造在核心库保留 `_paramNames` 期间（每步回归 pass）；Task 8 最后删核心库（sample 已无引用）。避免长编译失败期。

---

## 后续（本计划外）

- GUI `element_panel` 编辑 paramNames（makeData 自动建本构，UI 适配）
- preParser（macs 迁移）套用 makeData
- `test/truss1D/CDFEG/` 旧快照清理（.gitignore 或重生成）
